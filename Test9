-- ============================================================
-- MELAYU-HUB — BY SIGMA
-- ============================================================

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "MELAYU-HUB", LoadingTitle = "BY SIGMA",
   LoadingSubtitle = "LOADING...",
   ConfigurationSaving = { Enabled = true, FileName = "MelayuHub" },
   Discord = { Enabled = false }, KeySystem = false,
})

-- ============================================================
-- SERVICES
-- ============================================================
local Players           = game:GetService("Players")
local TweenService      = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService        = game:GetService("RunService")

local Player    = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local HRP       = Character:WaitForChild("HumanoidRootPart")
local Humanoid  = Character:WaitForChild("Humanoid")

local flySpeed    = 150
local mobDistance = 6
local activeTween = nil
local activeBV    = nil

local herbActive     = false
local treasureActive = false
local altarActive    = false
local mobActive      = false
local questActive    = false
local alchemyActive  = false
local harvestingActive = false
local slayingActive    = false
local questLoopHarvesting = false
local questLoopSlaying    = false

local farmPriority = { herb = 3, treasure = 2, altar = 1, mob = 4, boss = 5 }
local multiFarmThread    = nil
local mobFarmThread      = nil
local bossFarmThread     = nil
local combatFarmThread   = nil  -- handle mob+boss concurrent dengan priority
local slayingFarmThread  = nil
local harvestingFarmThread = nil

local selectedHerb          = nil
local herbRoundRobinIndex   = 1
local selectedMob       = nil
local selectedQuest     = nil
local selectedTreasure  = {}
local selectedBloodline = {}
local questLoopEnabled  = false
local justRespawned     = false

local herbCollectCount      = 0
local treasureCollectCount  = 0
local altarCollectCount     = 0

-- Pre-declared so CharacterAdded can reference them before Tab5 is created
local sqLblProg = nil
local hqLblProg = nil

local lastTreasureNotify = 0
local lastAltarNotify    = 0
local lastBossNotify     = 0
local NOTIFY_COOLDOWN    = 10

local function killAllFarmThreads()
    if activeBV and activeBV.Parent then
        pcall(function() activeBV.Velocity = Vector3.zero; activeBV:Destroy() end)
    end
    activeBV = nil
    if multiFarmThread   then pcall(function() task.cancel(multiFarmThread) end);   multiFarmThread   = nil; combatFarmThread = nil end
    if combatFarmThread  then pcall(function() task.cancel(combatFarmThread) end);  combatFarmThread  = nil end
    if slayingFarmThread then pcall(function() task.cancel(slayingFarmThread) end); slayingFarmThread = nil end
    if harvestingFarmThread then pcall(function() task.cancel(harvestingFarmThread) end); harvestingFarmThread = nil end
    if mobFarmThread     then pcall(function() task.cancel(mobFarmThread) end);     mobFarmThread     = nil end
    if bossFarmThread    then pcall(function() task.cancel(bossFarmThread) end);    bossFarmThread    = nil end
end

local function hookHumanoidDeath()
    if Humanoid then
        Humanoid.Died:Connect(function()
            killAllFarmThreads()
        end)
    end
end

hookHumanoidDeath()

Player.CharacterAdded:Connect(function(c)
    Character = c
    HRP       = c:WaitForChild("HumanoidRootPart")
    Humanoid  = c:WaitForChild("Humanoid")
    justRespawned = true

    hookHumanoidDeath()

    if HRP then
        HRP.Anchored = false
        HRP.AssemblyLinearVelocity  = Vector3.zero
        HRP.AssemblyAngularVelocity = Vector3.zero
    end
    if Humanoid then
        Humanoid.PlatformStand = false
        Humanoid.AutoRotate    = true
    end

    task.wait(2.5)
    justRespawned = false
    task.wait(0.3)

    if HRP and HRP.Parent then
        HRP.Anchored = false
        HRP.AssemblyLinearVelocity = Vector3.zero
    end

    -- Restart unified farm if any farm is active
    if anyFarmActive() then
        ensureUnifiedFarmRunning()
    end
    if slayingActive and selectedSlayingQuest then
        slayingFarmThread = task.spawn(function()
            while slayingActive do
                runSlayingCycle(sqLblProg)
                if not slayingActive then break end
                local prog, pEnd = getProgressFromServer(selectedSlayingQuest)
                if not prog or prog >= (pEnd or 0) then
                    if questLoopSlaying then acceptQuest(selectedSlayingQuest); task.wait(1)
                    else slayingActive = false; break end
                end
                task.wait(0.5)
            end
            stopFlyNoclip()
        end)
    end
    if harvestingActive and selectedHarvestingQuest then
        harvestingFarmThread = task.spawn(function()
            while harvestingActive do
                runHarvestingCycle(hqLblProg)
                if not harvestingActive then break end
                local prog, pEnd = getProgressFromServer(selectedHarvestingQuest)
                if not prog or prog >= (pEnd or 0) then
                    if questLoopHarvesting then acceptQuest(selectedHarvestingQuest); task.wait(1)
                    else harvestingActive = false; break end
                end
                task.wait(0.5)
            end
            stopFlyNoclip()
        end)
    end
end)

-- ============================================================
-- FLY + NOCLIP SYSTEM
-- ============================================================
local noclipEnabled = false

local noclipConnection = RunService.Stepped:Connect(function()
    if noclipEnabled and Character then
        for _, part in ipairs(Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

local function setNoclip(state)
    noclipEnabled = state
    if not state and Character then
        for _, part in ipairs(Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
    end
end

local function flyTo(targetPos, activeCheck)
    if not HRP or not HRP.Parent or not Humanoid then return false end

    setNoclip(true)
    if HRP.Anchored then
        HRP.Anchored = false
        HRP.AssemblyLinearVelocity = Vector3.zero
    end

    local bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    bv.Velocity  = Vector3.zero
    bv.Parent    = HRP
    activeBV     = bv

    local maxTime = 60
    local t0      = tick()
    local reached = false

    while tick() - t0 < maxTime do
        if activeCheck and not activeCheck() then break end
        if not HRP or not HRP.Parent then break end
        if not bv or not bv.Parent then reached = false; break end

        local currentPos = HRP.Position
        local dist       = (targetPos - currentPos).Magnitude

        if dist <= 3 then
            reached = true
            break
        end

        local dir = (targetPos - currentPos).Unit
        local speed
        if dist <= 15 then
            speed = math.max(10, flySpeed * (dist / 15))
        else
            speed = flySpeed
        end

        bv.Velocity = dir * speed
        task.wait()
    end

    if bv and bv.Parent then
        bv.Velocity = Vector3.zero
        task.wait(0.05)
        bv:Destroy()
    end
    activeBV = nil

    if reached and HRP and HRP.Parent then
        HRP.AssemblyLinearVelocity = Vector3.zero
        HRP.AssemblyAngularVelocity = Vector3.zero
        HRP.Anchored = true
        task.wait(0.15)
        if HRP and HRP.Parent then
            HRP.Anchored = false
            HRP.AssemblyLinearVelocity = Vector3.zero
        end
    end

    return reached
end

local function stopFlyNoclip()
    local inCombat = bossActive or mobActive

    if activeBV and activeBV.Parent then
        activeBV.Velocity = Vector3.zero
        activeBV:Destroy()
    end
    activeBV = nil

    if HRP then
        for _, bv in ipairs(HRP:GetChildren()) do
            if bv:IsA("BodyVelocity") then
                bv.Velocity = Vector3.zero
                bv:Destroy()
            end
        end
    end

    if not inCombat then
        setNoclip(false)
        if Humanoid then
            Humanoid.PlatformStand = false
            Humanoid.AutoRotate    = true
        end
        if HRP and HRP.Parent then
            HRP.AssemblyLinearVelocity  = Vector3.zero
            HRP.AssemblyAngularVelocity = Vector3.zero
            HRP.Anchored = true
            task.wait(0.15)
            if HRP and HRP.Parent then
                HRP.Anchored = false
                HRP.AssemblyLinearVelocity  = Vector3.zero
                HRP.AssemblyAngularVelocity = Vector3.zero
            end
        end
        task.spawn(function()
            local t0 = tick()
            while tick() - t0 < 0.6 do
                if bossActive or mobActive then return end
                if Humanoid and Humanoid.Parent then
                    Humanoid.PlatformStand = false
                    Humanoid.AutoRotate    = true
                end
                if HRP and HRP.Parent then
                    HRP.AssemblyLinearVelocity  = Vector3.zero
                    HRP.AssemblyAngularVelocity = Vector3.zero
                end
                task.wait(0.05)
            end
            if Humanoid and Humanoid.Parent then
                pcall(function() Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end)
                task.wait(0.05)
                pcall(function() Humanoid:ChangeState(Enum.HumanoidStateType.Running) end)
            end
        end)
    else
        if HRP and HRP.Parent then
            HRP.AssemblyLinearVelocity  = Vector3.zero
            HRP.AssemblyAngularVelocity = Vector3.zero
        end
    end
end

local function snapToGround()
    if not HRP or not HRP.Parent or not Character then return end
    setNoclip(false)
    if Humanoid then
        Humanoid.PlatformStand = false
        Humanoid.AutoRotate    = true
    end
    HRP.Anchored = false
    HRP.AssemblyLinearVelocity  = Vector3.zero
    HRP.AssemblyAngularVelocity = Vector3.zero
    for _, part in ipairs(Character:GetDescendants()) do
        if part:IsA("BasePart") then part.CanCollide = true end
    end
    task.wait(0.1)
    if Humanoid then
        pcall(function() Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end)
        task.wait(0.05)
        pcall(function() Humanoid:ChangeState(Enum.HumanoidStateType.Running) end)
    end
end

-- ============================================================
-- YEAR / ALCHEMY DATA
-- ============================================================
local YEAR_DISPLAY_LIST = {
    "One Year Old","Ten Year Old","Hundred Year Old","Thousand Year Old",
    "Ten Thousand Year Old","One Hundred Thousand Year Old",
    "Million Year Old","Ten Million Year Old",
}
local YEAR_TO_VARIATION = {
    ["One Year Old"]="One",["Ten Year Old"]="Ten",["Hundred Year Old"]="Hundred",
    ["Thousand Year Old"]="Thousand",["Ten Thousand Year Old"]="Ten Thousand",
    ["One Hundred Thousand Year Old"]="Hundred Thousand",
    ["Million Year Old"]="Million",["Ten Million Year Old"]="Ten Million",
}
local YEAR_COLOURS = {
    ["One"]=Color3.fromRGB(200,200,200),["Ten"]=Color3.fromRGB(150,230,150),
    ["Hundred"]=Color3.fromRGB(100,210,100),["Thousand"]=Color3.fromRGB(100,150,255),
    ["Ten Thousand"]=Color3.fromRGB(180,100,255),["Hundred Thousand"]=Color3.fromRGB(255,210,50),
    ["Million"]=Color3.fromRGB(255,130,50),["Ten Million"]=Color3.fromRGB(255,60,60),
}

local selectedAlchemyYear = "Thousand"
local alchemyCraftAmount  = 1

local HERB_ELEMENT = {
    ["Ginseng"]="Earth",["Taixian"]="Earth",["Qilin Berries"]="Heaven",
    ["Moonlight Flower"]="Heaven",["Twilight Bloom"]="Yin",["Spirit Grass"]="Yin",
    ["Blood Flower"]="Yin",["Yin Bush"]="Yin",["Lingzhi"]="Yang",["Yang Flower"]="Yang",
}

local ALCHEMY_RECIPES = {
    ["Breakthrough Pill"]    = {Base=40,  Elements={"Earth","Earth","Earth","Earth"},           Herbs={"Ginseng","Ginseng","Ginseng","Ginseng"},                     Requirement="Mortal I+ (Qi Rank 0+)"},
    ["Restoration Pill"]     = {Base=50,  Elements={"Earth","Earth","Earth","Yang"},            Herbs={"Ginseng","Ginseng","Ginseng","Yang Flower"},                 Requirement="Body Refining skill learned"},
    ["Health Pill"]          = {Base=80,  Elements={"Yin","Earth","Yang","Earth"},              Herbs={"Yin Bush","Ginseng","Yang Flower","Ginseng"},                Requirement="Qi Fusion I+ (Qi Rank 20+)"},
    ["Qi Pill"]              = {Base=80,  Elements={"Heaven","Heaven","Earth","Yin"},           Herbs={"Qilin Berries","Qilin Berries","Ginseng","Yin Bush"},        Requirement="True Foundation I+ (Qi Rank 40+)"},
    ["Molian Pill"]          = {Base=150, Elements={"Yin","Yin","Yang","Heaven"},               Herbs={"Yin Bush","Yin Bush","Yang Flower","Qilin Berries"},         Requirement="Core Formation I+ (Qi Rank 50+)"},
    ["Shulian Pill"]         = {Base=150, Elements={"Heaven","Heaven","Heaven","Heaven"},       Herbs={"Qilin Berries","Qilin Berries","Qilin Berries","Qilin Berries"},Requirement="Immortal I+ (Qi Rank 100+)"},
    ["Dragon Pill"]          = {Base=250, Elements={"Earth","Heaven","Yin","Yang"},             Herbs={"Ginseng","Qilin Berries","Yin Bush","Yang Flower"},          Requirement="Give 100k Year herb of each element to Shao Ruogang"},
    ["Tian Pill"]            = {Base=275, Elements={"Earth","Heaven","Heaven","Earth"},         Herbs={"Ginseng","Qilin Berries","Qilin Berries","Ginseng"},         Requirement="Creation Realm I+ (Qi Rank 200+)"},
    ["Soul Breakthrough Pill"]={Base=300, Elements={"Yin","Yang","Yin","Yang"},                 Herbs={"Yin Bush","Yang Flower","Yin Bush","Yang Flower"},           Requirement="1 Ascension"},
    ["Soul Energy Pill"]     = {Base=300, Elements={"Yin","Yin","Yang","Yang"},                 Herbs={"Yin Bush","Yin Bush","Yang Flower","Yang Flower"},           Requirement="3 Ascensions"},
    ["Soul Chance Pill"]     = {Base=400, Elements={"Yang","Yang","Yin","Yin"},                 Herbs={"Yang Flower","Yang Flower","Yin Bush","Yin Bush"},           Requirement="1 Ascension"},
    ["Soul Cap Pill"]        = {Base=300, Elements={"Yang","Yin","Yang","Yin"},                 Herbs={"Yang Flower","Yin Bush","Yang Flower","Yin Bush"},           Requirement="10 Ascensions"},
}

local recipeNames = {}
for name in pairs(ALCHEMY_RECIPES) do table.insert(recipeNames,name) end
table.sort(recipeNames)
local selectedRecipe = nil

local ELEMENT_HERBS = {
    Earth   = {"Taixian","Ginseng"},
    Heaven  = {"Moonlight Flower","Qilin Berries"},
    Yin     = {"Yin Bush","Blood Flower","Twilight Bloom","Spirit Grass"},
    Yang    = {"Yang Flower","Lingzhi"},
}

local HERB_BASE_VALUE = {
    ["Ginseng"]=8,["Taixian"]=18,["Qilin Berries"]=23,["Moonlight Flower"]=25,
    ["Twilight Bloom"]=30,["Blood Flower"]=35,["Yang Flower"]=45,["Yin Bush"]=50,
    ["Lingzhi"]=15,["Spirit Grass"]=16,
}

local YEAR_MULTIPLIER = {
    ["One"]=1,["Ten"]=2,["Hundred"]=4,["Thousand"]=8,["Ten Thousand"]=16,
    ["Hundred Thousand"]=32,["Million"]=64,["Ten Million"]=128,
}

local selectedElementHerb = {}

local function getBestHerbForElement(element)
    if selectedElementHerb[element] then return selectedElementHerb[element] end
    local herbs = ELEMENT_HERBS[element]
    return herbs and herbs[1] or "Ginseng"
end

local function calcRecipeBase(recipe, yearVariation)
    local total = 0
    local mult  = YEAR_MULTIPLIER[yearVariation] or 1
    for _, element in ipairs(recipe.Elements) do
        local herb = getBestHerbForElement(element)
        local base = HERB_BASE_VALUE[herb] or 8
        total += base * mult
    end
    return total
end

-- ============================================================
-- BLOODLINE DATA
-- ============================================================
local BLOODLINE_DATA = {
    ["Forbidden Apple"]=1,["Spider"]=1,["Stone Monkey"]=1,
    ["Turtle"]=2,["Wolf"]=2,["Giant"]=2,
    ["Jade Monkey"]=3,["Fairy"]=3,["Fox"]=3,
    ["True Phoenix"]=4,["Eternal Ice"]=4,["Heavenly Demon"]=4,
    ["Stellar Monarch"]=4,["Primordial Beast"]=4,
    ["Azure Dragon"]=5,["Immortal"]=5,
}
local bloodlineDropdownList = {"All Bloodlines"}
do
    local sorted={}
    for name,tier in pairs(BLOODLINE_DATA) do table.insert(sorted,{name=name,tier=tier}) end
    table.sort(sorted,function(a,b) if a.tier~=b.tier then return a.tier<b.tier end; return a.name<b.name end)
    for _, v in ipairs(sorted) do table.insert(bloodlineDropdownList,string.format("[T%d] %s",v.tier,v.name)) end
end

-- ============================================================
-- TREASURE CATEGORIES
-- ============================================================
local TREASURE_CATEGORIES = {
    "All Treasures","Common Treasure","Lost Treasure","Rare Treasure","Very Rare Treasure",
}

-- ============================================================
-- QUEST DATA
-- ============================================================
local QUEST_DATA = {
    ["Bei Fang"]        = {Type="Slaying",    Target="Bandit",                 Amount=5  },
    ["Xi Hao"]          = {Type="Slaying",    Target="Commoner",               Amount=5  },
    ["Yang Wei"]        = {Type="Slaying",    Target="Female Blood Cultivator",Amount=7  },
    ["Taoist Xi"]       = {Type="Slaying",    Target="Furious Sect Member",    Amount=6  },
    ["Sun Tao"]         = {Type="Slaying",    Target="Male Assassin",          Amount=8  },
    ["Gao Xiang"]       = {Type="Slaying",    Target="Sect Master",            Amount=3  },
    ["Tong Li"]         = {Type="Slaying",    Target="Male Blood Cultivator",  Amount=6  },
    ["Wu Hao"]          = {Type="Slaying",    Target="Bao Laohu",              Amount=1  },
    ["Xia Hui"]         = {Type="Slaying",    Target="Corrupt Commoner",       Amount=7  },
    ["Zhing Xiu"]       = {Type="Slaying",    Target="Corrupt Minion",         Amount=7  },
    ["Xian Ling"]       = {Type="Slaying",    Target="Corrupt Admiral",        Amount=6  },
    ["Yang Zhu"]        = {Type="Slaying",    Target="Mount Hua Leader",       Amount=1  },
    ["Ling Qi"]         = {Type="Slaying",    Target="Stone Head Disciple",    Amount=6  },
    ["Xumian Shidi"]    = {Type="Slaying",    Target="Slatebound Disciple",    Amount=12 },
    ["Xumian Shixiong"] = {Type="Slaying",    Target="Slatebound Elder",       Amount=6  },
    ["Yuzhou"]          = {Type="Slaying",    Target="Slatebound Sovereign",   Amount=8  },
    ["Mo Gui"]          = {Type="Slaying",    Target="Professional Guard",     Amount=50,  Path="Demon"},
    ["Mo Yan"]          = {Type="Slaying",    Target="Loyal Guard",            Amount=100, Path="Demon"},
    ["Hongzhu Dashi"]   = {Type="Slaying",    Target="Diyu Mogui",             Amount=7,  Path="Heavenly"},
    ["Shemni Nuzhu"]    = {Type="Slaying",    Target="Duyao Zhanshen",         Amount=8,  Path="Demonic" },
    ["Liehuo Jiangjun"] = {Type="Slaying",    Target="Siwang Zhi Zhang",       Amount=9,  Path="Demonic" },
    ["Xiao Ling"]       = {Type="Harvesting", Target="Ginseng",                Amount=5  },
    ["Zhang Weiwei"]    = {Type="Harvesting", Target="Yin Bush",               Amount=100},
    ["Zhang Xiaoxiao"]  = {Type="Harvesting", Target="Yin Bush",               Amount=50 },
    ["Zhang Jingjing"]  = {Type="Harvesting", Target="Yang Flower",            Amount=50 },
    ["Zhang Xinxin"]    = {Type="Harvesting", Target="Yang Flower",            Amount=100},
    ["Zhang Yiwang"]    = {Type="Harvesting", Target="Blood Flower",           Amount=250},
    ["Yiwang Yaotian"]  = {Type="Harvesting", Target="Blood Flower",           Amount=500},
}

local slayingQuestNames, harvestingQuestNames = {}, {}
for name, info in pairs(QUEST_DATA) do
    if info.Type == "Slaying"    then table.insert(slayingQuestNames,    name) end
    if info.Type == "Harvesting" then table.insert(harvestingQuestNames, name) end
end
table.sort(slayingQuestNames)
table.sort(harvestingQuestNames)
local questNamesList={}
for name in pairs(QUEST_DATA) do table.insert(questNamesList,name) end
table.sort(questNamesList)

local BOSS_MOBS = {"Zanshi Huo Ren", "Zanshi Bing Ren"}
local selectedBosses = {"Zanshi Huo Ren", "Zanshi Bing Ren"}
local bossActive   = false
local bossNotifyEnabled = true

-- ============================================================
-- STOP ALL
-- ============================================================
local function stopAll()
    herbActive=false; treasureActive=false; altarActive=false
    mobActive=false; questActive=false; alchemyActive=false; bossActive=false
    pcall(function() slayingActive=false end)
    pcall(function() harvestingActive=false end)
    if activeTween then activeTween:Cancel(); activeTween=nil end
    herbCollectCount=0; treasureCollectCount=0; altarCollectCount=0
    if multiFarmThread then
        pcall(function() task.cancel(multiFarmThread) end)
        multiFarmThread = nil
        combatFarmThread = nil
    end
    if combatFarmThread then
        pcall(function() task.cancel(combatFarmThread) end)
        combatFarmThread = nil
    end
    if mobFarmThread then pcall(function() task.cancel(mobFarmThread) end); mobFarmThread = nil end
    if bossFarmThread then pcall(function() task.cancel(bossFarmThread) end); bossFarmThread = nil end
    if slayingFarmThread then pcall(function() task.cancel(slayingFarmThread) end); slayingFarmThread = nil end
    if harvestingFarmThread then pcall(function() task.cancel(harvestingFarmThread) end); harvestingFarmThread = nil end
    stopFlyNoclip()
    task.wait(0.5)
    snapToGround()
end

-- ============================================================
-- BOSS DETECTION SYSTEM
-- ============================================================
local function isBossMob(name)
    for _, b in ipairs(BOSS_MOBS) do if b == name then return true end end
    return false
end

local function playBossAlert()
    if not bossNotifyEnabled then return end
    local sound = Instance.new("Sound")
    sound.SoundId  = "rbxassetid://4590662766"
    sound.Volume   = 1
    sound.RollOffMaxDistance = 0
    sound.Parent   = workspace
    sound:Play()
    game:GetService("Debris"):AddItem(sound, 5)
end

task.spawn(function()
    task.wait(2)
    local enemiesFolder = workspace:FindFirstChild("Enemies")
    if not enemiesFolder then return end
    for _, mob in pairs(enemiesFolder:GetChildren()) do
        if isBossMob(mob.Name) then
            playBossAlert()
            if bossNotifyEnabled then
                Rayfield:Notify({Title="Mob Detected",Content=mob.Name.." already spawned!",Duration=6, Image=4483362458}))
            end
        end
    end
end)

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Enemies" then
        child.ChildAdded:Connect(function(mob)
            if isBossMob(mob.Name) then
                playBossAlert()
                if bossNotifyEnabled then
                    Rayfield:Notify({Title="Mob Detected",Content=mob.Name.." has spawned!",Duration=8, Image=4483362458}))
                end
            end
        end)
    end
end)

do
    local enemiesFolder = workspace:FindFirstChild("Enemies")
    if enemiesFolder then
        enemiesFolder.ChildAdded:Connect(function(mob)
            if isBossMob(mob.Name) then
                playBossAlert()
                if bossNotifyEnabled then
                    Rayfield:Notify({Title="Mob Detected",Content=mob.Name.." has spawned!",Duration=8, Image=4483362458}))
                end
            end
        end)
    end
end

-- ============================================================
-- HELPERS
-- ============================================================
local function getCF(obj)
    if not obj then return nil end
    if typeof(obj)=="CFrame" then return obj end
    local cf
    pcall(function()
        if obj:IsA("BasePart") then
            cf = obj.CFrame
        elseif obj:IsA("Model") then
            -- PrimaryPart dulu — GetPivot() boleh return Y gila kalau model ada part tinggi
            if obj.PrimaryPart then
                cf = obj.PrimaryPart.CFrame
            else
                local p = obj:FindFirstChildWhichIsA("BasePart", true)
                if p then cf = p.CFrame else cf = obj:GetPivot() end
            end
        elseif obj:IsA("Folder") then
            local p = obj:FindFirstChildWhichIsA("BasePart", true)
            if p then cf = p.CFrame end
        else
            local p = obj:FindFirstChildWhichIsA("BasePart", true)
            if p then cf = p.CFrame else pcall(function() cf = obj:GetPivot() end) end
        end
    end)
    return cf
end

local function isPlayerInZone(zoneObj)
    if not zoneObj or not HRP then return false end
    local zonePart=zoneObj:IsA("BasePart") and zoneObj or zoneObj:FindFirstChildWhichIsA("BasePart",true)
    if not zonePart then return false end
    local dist=(HRP.Position-zonePart.Position).Magnitude
    local maxD=math.max(zonePart.Size.X,zonePart.Size.Y,zonePart.Size.Z)/2
    return dist<=maxD+2
end

local function getHerbVariation(obj)
    local v=nil
    pcall(function() v=obj:GetAttribute("Variation") end)
    if not v then pcall(function() local vc=obj:FindFirstChild("Variation"); if vc then v=vc.Value end end) end
    if not v then
        local n=obj.Name or ""
        for _, yr in ipairs({"Ten Million","Million","Hundred Thousand","Ten Thousand","Thousand","Hundred","Ten","One"}) do
            if n:find(yr) then v=yr; break end
        end
    end
    return v or "One"
end

local function findRandomHerb(folder,minDistance,yearFilter)
    if not folder or not HRP then return nil,0 end
    minDistance=minDistance or 5
    local valid={}
    for _, obj in pairs(folder:GetChildren()) do
        if not obj.Parent then continue end
        if yearFilter and getHerbVariation(obj)~=yearFilter then continue end
        local pos
        pcall(function()
            if obj:IsA("Model") then pos=obj:GetPivot().Position
            elseif obj:IsA("BasePart") then pos=obj.Position
            elseif obj:IsA("Folder") then local p=obj:FindFirstChildWhichIsA("BasePart",true); if p then pos=p.Position end end
        end)
        if pos then
            local d=(HRP.Position-pos).Magnitude
            if d>=minDistance then table.insert(valid,{obj=obj,dist=d}) end
        end
    end
    if #valid>0 then local c=valid[math.random(1,#valid)]; return c.obj,c.dist end
    return nil,0
end

local function findNearest(folder,minDistance,filterFunc)
    if not folder or not HRP then return nil,math.huge end
    minDistance=minDistance or 0
    local nearest,shortest=nil,math.huge
    for _, obj in pairs(folder:GetChildren()) do
        if not obj.Parent then continue end
        if filterFunc and not filterFunc(obj) then continue end
        local pos
        pcall(function()
            if obj:IsA("Model") then pos=obj:GetPivot().Position
            elseif obj:IsA("BasePart") then pos=obj.Position
            elseif obj:IsA("Folder") then local p=obj:FindFirstChildWhichIsA("BasePart",true); if p then pos=p.Position end end
        end)
        if pos then
            local d=(HRP.Position-pos).Magnitude
            if d>=minDistance and d<shortest then shortest=d; nearest=obj end
        end
    end
    return nearest,shortest
end

-- ============================================================
-- HARVEST PROGRESS BAR
-- ============================================================
local function getProgressBar()
    local gui=Player.PlayerGui
    local hp=gui:FindFirstChild("harvestPrompt")
    if hp then local pb=hp:FindFirstChild("ProgressBar"); if pb then local p=pb:FindFirstChild("Progress"); return p or pb end end
    for _, v in pairs(gui:GetDescendants()) do
        if v.Name=="Progress" and v.Parent and v.Parent.Name=="ProgressBar" then return v end
    end
    return nil
end

local function promptVisible()
    local hp = Player.PlayerGui:FindFirstChild("harvestPrompt")
    return hp and hp.Enabled == true
end

local function waitForHarvestComplete(activeCheck)
    -- Wait for prompt to appear (1.5s max)
    local t0 = tick()
    while tick() - t0 < 1.5 and activeCheck() do
        if promptVisible() then break end
        task.wait(0.02)
    end
    if not promptVisible() then return "timeout" end

    -- Prompt visible — check progress bar is actually moving within 1s
    local bar = getProgressBar()
    if bar then
        local s0 = 0; pcall(function() s0 = bar.Size.X.Scale end)
        local tMove = tick()
        local moved = false
        while tick() - tMove < 1 and activeCheck() do
            if not promptVisible() then return "complete" end
            local s1 = 0; pcall(function() s1 = bar.Size.X.Scale end)
            if math.abs(s1 - s0) > 0.02 then moved = true; break end
            task.wait(0.02)
        end
        if not moved then return "stuck" end
    end

    -- Wait for prompt to disappear — prompt gone = done
    while activeCheck() do
        if not promptVisible() then return "complete" end
        task.wait(0.02)
    end
    return "stop"
end

local function getHerbFolder(name)
    local herbs=workspace:FindFirstChild("Herbs"); if not herbs then return nil end
    local node=herbs:FindFirstChild(name); if not node then return nil end
    return node:FindFirstChild("Folder") or node
end

-- ============================================================
-- HERB FARMING
-- ============================================================
local herbZoneIndices = {}  -- per-herb zone index for interleaved round robin

local function doOneHerb(extraCheck)
    local function isActive()
        if not herbActive then return false end
        if extraCheck and not extraCheck() then return false end
        return true
    end

    local list = type(selectedHerb) == "table" and selectedHerb or (selectedHerb and {selectedHerb} or {})
    if not list or #list == 0 then return false end

    -- Advance herb round robin — ONE herb per call only, no fallback loop
    if herbRoundRobinIndex > #list then herbRoundRobinIndex = 1 end
    local herbName = list[herbRoundRobinIndex]
    herbRoundRobinIndex = (herbRoundRobinIndex % #list) + 1

    -- Collect all zones for this herb
    local folder = getHerbFolder(herbName)
    if not folder then return false end

    local allZones = {}
    for _, child in ipairs(folder:GetChildren()) do
        local z = child:FindFirstChild("HerbZone", true)
        if z and z:IsA("BasePart") and z.Parent then
            table.insert(allZones, z)
        end
    end
    if #allZones == 0 then return false end

    -- Advance zone index for this herb
    if not herbZoneIndices[herbName] or herbZoneIndices[herbName] > #allZones then
        herbZoneIndices[herbName] = 1
    end
    local zone = allZones[herbZoneIndices[herbName]]
    herbZoneIndices[herbName] = (herbZoneIndices[herbName] % #allZones) + 1

    if not zone or not zone.Parent then return false end

    local targetCF = getCF(zone)
    if not targetCF then return false end
    local zonePos = targetCF.Position
    local safePos = Vector3.new(zonePos.X, zonePos.Y + 1.5, zonePos.Z)

    local reached = flyTo(safePos, isActive)
    if not isActive() then return false end
    setNoclip(true)

    if not reached then
        local zCF = getCF(zone)
        if zCF and HRP and HRP.Parent then
            HRP.CFrame = CFrame.new(zCF.Position.X, zCF.Position.Y + 1.5, zCF.Position.Z)
            HRP.AssemblyLinearVelocity = Vector3.zero
            task.wait(0.1)
        end
    end

    local attempt = 0
    while not isPlayerInZone(zone) and attempt < 20 and isActive() do
        attempt += 1
        local zCF = getCF(zone)
        if zCF then
            HRP.CFrame = CFrame.new(zCF.Position.X, zCF.Position.Y + 1.5, zCF.Position.Z)
            HRP.AssemblyLinearVelocity = Vector3.zero
        end
        task.wait(0.05)
    end
    if not isPlayerInZone(zone) or not isActive() then return false end

    task.wait(0.2)
    if not isPlayerInZone(zone) or not isActive() then return false end

    if HRP then HRP.Anchored = true end
    setNoclip(true)
    pcall(function() ReplicatedStorage.RemoteEvents.Harvest:FireServer() end)

    local result = waitForHarvestComplete(isActive)
    if HRP then
        HRP.Anchored = false
        HRP.AssemblyLinearVelocity = Vector3.zero
    end

    if result == "complete" then
        herbCollectCount += 1
        return true
    end
    -- timeout / stuck / stop — skip and move on
    task.wait(0.2)
    return false
end

-- ============================================================
-- TREASURE FARMING
-- ============================================================
local function firePrompt(obj)
    local kh = obj:FindFirstChild("KeyHole")
    if kh then
        local p = kh:FindFirstChild("OpenPrompt")
        if p and p:IsA("ProximityPrompt") then
            local old = p.HoldDuration; p.HoldDuration = 0
            fireproximityprompt(p); p.HoldDuration = old; return true
        end
    end
    local p = obj:FindFirstChildOfClass("ProximityPrompt", true)
    if p then
        local old = p.HoldDuration; p.HoldDuration = 0
        fireproximityprompt(p); p.HoldDuration = old; return true
    end
    return false
end

local function treasureMatchesFilter(obj)
    if not selectedTreasure or #selectedTreasure == 0 then return true end
    for _, pick in ipairs(selectedTreasure) do
        if pick == "All Treasures" then return true end
        local nameLen = #pick
        if obj.Name:sub(1, nameLen) == pick then
            local nextChar = obj.Name:sub(nameLen + 1, nameLen + 1)
            if nextChar == "" or nextChar == " " or tonumber(nextChar) then return true end
        end
    end
    return false
end

local function doOneTreasure(extraCheck)
    local function isActive()
        if not treasureActive then return false end
        if extraCheck and not extraCheck() then return false end
        return true
    end
    local folder = workspace:FindFirstChild("Treasures")
    if not folder then return false end
    local treasure = findNearest(folder, 0, treasureMatchesFilter)
    if not treasure then
        if tick() - lastTreasureNotify >= NOTIFY_COOLDOWN then
            local label = (selectedTreasure and #selectedTreasure > 0) and table.concat(selectedTreasure, ", ") or "All Treasures"
            Rayfield:Notify({Title="Treasure Farm", Content="No "..label.." found yet", Duration=3, Image=4483362458}))
            lastTreasureNotify = tick()
        end
        return false
    end
    local cf = getCF(treasure)
    if not cf then return false end
    local reached = flyTo(cf.Position, isActive)
    if not reached or not treasure.Parent or not isActive() then return false end
    setNoclip(true)
    task.wait(0.3)
    local hasPrompt = false; local count = 0
    while treasure.Parent and isActive() and count < 400 do
        local curCF = getCF(treasure)
        if curCF and (HRP.Position - curCF.Position).Magnitude > 20 then
            flyTo(curCF.Position, isActive)
            task.wait(0.2)
        end
        if firePrompt(treasure) then hasPrompt = true end
        count += 1; task.wait(0.05)
    end
    if not treasure.Parent then treasureCollectCount += 1; return true end
    return false
end

-- ============================================================
-- ALTAR FARMING
-- ============================================================
local function fireAltarPrompt(altar)
    for _, d in pairs(altar:GetDescendants()) do
        if d:IsA("ProximityPrompt") and d.Name:lower():find("collect") then
            local old = d.HoldDuration; d.HoldDuration = 0
            fireproximityprompt(d); d.HoldDuration = old; return true
        end
    end
    local p = altar:FindFirstChildOfClass("ProximityPrompt", true)
    if p then
        local old = p.HoldDuration; p.HoldDuration = 0
        fireproximityprompt(p); p.HoldDuration = old; return true
    end
    return false
end

local function altarMatchesBloodline(altar)
    if not selectedBloodline or #selectedBloodline == 0 then return true end
    local al = altar.Name:lower()
    local bv = altar:FindFirstChild("Bloodline")
    local attr = nil; pcall(function() attr = altar:GetAttribute("Bloodline") end)
    for _, pick in ipairs(selectedBloodline) do
        local bl = pick:lower()
        if al:find(bl, 1, true) then return true end
        if bv and bv.Value and bv.Value:lower() == bl then return true end
        if attr and attr:lower() == bl then return true end
    end
    return false
end

local function doOneAltar(extraCheck)
    local function isActive()
        if not altarActive then return false end
        if extraCheck and not extraCheck() then return false end
        return true
    end
    local folder = workspace:FindFirstChild("Altars")
    if not folder then return false end
    local nearest = findNearest(folder, 0, altarMatchesBloodline)
    if not nearest then
        if tick() - lastAltarNotify >= NOTIFY_COOLDOWN then
            Rayfield:Notify({Title="Altar Farm", Content="No altar has been found yet", Duration=3, Image=4483362458}))
            lastAltarNotify = tick()
        end
        return false
    end
    local cf = getCF(nearest)
    if not cf then return false end
    -- Y+3 offset supaya fly ke atas/tepi altar, bukan bawah objek
    local targetPos = Vector3.new(cf.Position.X, cf.Position.Y + 3, cf.Position.Z)
    local reached = flyTo(targetPos, isActive)
    if not reached or not isActive() then return false end
    task.wait(0.5)
    fireAltarPrompt(nearest)
    task.wait(0.5)
    altarCollectCount += 1
    return true
end

local function stopCultivatingIfNeeded()
    local state = Character and Character:FindFirstChild("State")
    if not state then return end
    local isCult = state:FindFirstChild("IsCultivating")
    if not isCult then return end
    if isCult.Value == true then
        pcall(function() ReplicatedStorage.RemoteEvents.Cultivate:FireServer() end)
        local t0 = tick()
        while isCult.Value == true and tick() - t0 < 3 do
            task.wait(0.05)
        end
        if isCult.Value == true then
            pcall(function() ReplicatedStorage.RemoteEvents.Cultivate:FireServer() end)
            task.wait(0.3)
        end
    end
end

local function getCombatCFrame(mobRoot)
    local abovePos = mobRoot.Position + Vector3.new(0, mobDistance, 0)
    return CFrame.new(abovePos, abovePos + mobRoot.CFrame.LookVector) * CFrame.Angles(-math.pi/2, 0, 0)
end

-- ============================================================
-- UNIFIED FARM COORDINATOR — single thread, priority queue
-- ONE action per farm per cycle → no two flyTo calls conflict
-- Herb/treasure/altar: one zone per turn
-- Mob: fly + kill ONE mob per turn, loop back if more alive
-- Boss: fly + kill ONE boss per turn, loop back if more alive
-- ============================================================

local function anyFarmActive()
    return herbActive or treasureActive or altarActive or mobActive or bossActive
end

local function doOneMob()
    -- Fly to nearest mob, kill it. Return true = killed/found, false = none.
    local folder = workspace:FindFirstChild("Enemies")
    if not folder or not selectedMob then return false end
    local nearest, shortest = nil, math.huge
    for _, mob in pairs(folder:GetChildren()) do
        if mob.Name == selectedMob then
            local hum = mob:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local ok, pos = pcall(function() return mob:GetPivot().Position end)
                if ok then
                    local d = (HRP.Position - pos).Magnitude
                    if d < shortest then shortest = d; nearest = mob end
                end
            end
        end
    end
    if not nearest then return false end
    local mobRoot = nearest:FindFirstChild("HumanoidRootPart")
    if not mobRoot then return false end
    flyTo(getCombatCFrame(mobRoot).Position, function() return mobActive end)
    task.wait(0.05)
    if not nearest.Parent then return true end
    local hum = nearest:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return true end
    Humanoid.PlatformStand = true
    while nearest.Parent and mobActive do
        if justRespawned then break end
        local h = nearest:FindFirstChildOfClass("Humanoid")
        if not h or h.Health <= 0 then break end
        if mobRoot and mobRoot.Parent then
            HRP.CFrame = getCombatCFrame(mobRoot)
            HRP.AssemblyLinearVelocity  = Vector3.zero
            HRP.AssemblyAngularVelocity = Vector3.zero
        end
        stopCultivatingIfNeeded()
        local h2 = nearest:FindFirstChildOfClass("Humanoid")
        if not h2 or h2.Health <= 0 then break end
        pcall(function() ReplicatedStorage.RemoteEvents.Attack:FireServer("Light", {["RootPart"]=HRP}) end)
        task.wait(0.3)
    end
    Humanoid.PlatformStand = false
    Humanoid.AutoRotate = true
    if HRP and HRP.Parent then
        HRP.AssemblyLinearVelocity  = Vector3.zero
        HRP.AssemblyAngularVelocity = Vector3.zero
    end
    setNoclip(true)
    return true
end

local function doOneBoss()
    -- Fly to nearest boss, kill it. Return true = killed/found, false = none.
    local nearest = findBoss()
    if not nearest then return false end
    local mobRoot = nearest:FindFirstChild("HumanoidRootPart")
    if not mobRoot then return false end
    flyTo(getCombatCFrame(mobRoot).Position, function()
        return bossActive and findBoss() ~= nil
    end)
    task.wait(0.05)
    if not nearest.Parent then return true end
    local hum = nearest:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return true end
    Humanoid.PlatformStand = true
    while nearest.Parent and bossActive do
        if justRespawned then break end
        local h = nearest:FindFirstChildOfClass("Humanoid")
        if not h or h.Health <= 0 then break end
        if mobRoot and mobRoot.Parent then
            HRP.CFrame = getCombatCFrame(mobRoot)
            HRP.AssemblyLinearVelocity  = Vector3.zero
            HRP.AssemblyAngularVelocity = Vector3.zero
        end
        stopCultivatingIfNeeded()
        local h2 = nearest:FindFirstChildOfClass("Humanoid")
        if not h2 or h2.Health <= 0 then break end
        pcall(function() ReplicatedStorage.RemoteEvents.Attack:FireServer("Light", {["RootPart"]=HRP}) end)
        task.wait(0.3)
    end
    Humanoid.PlatformStand = false
    Humanoid.AutoRotate = true
    if HRP and HRP.Parent then
        HRP.AssemblyLinearVelocity  = Vector3.zero
        HRP.AssemblyAngularVelocity = Vector3.zero
    end
    setNoclip(true)
    return true
end

local function runUnifiedFarm()
    setNoclip(true)
    while anyFarmActive() do
        if justRespawned then
            while justRespawned and anyFarmActive() do task.wait(0.5) end
            if not anyFarmActive() then break end
            setNoclip(true)
        end
        if not HRP or not HRP.Parent then task.wait(0.5); continue end

        -- Build priority queue from ALL active farms
        local queue = {}
        if altarActive    then table.insert(queue, {t="altar",    p=farmPriority.altar,    i=1}) end
        if treasureActive then table.insert(queue, {t="treasure", p=farmPriority.treasure, i=2}) end
        if herbActive     then table.insert(queue, {t="herb",     p=farmPriority.herb,     i=3}) end
        if mobActive      then table.insert(queue, {t="mob",      p=farmPriority.mob,      i=4}) end
        if bossActive     then table.insert(queue, {t="boss",     p=farmPriority.boss,     i=5}) end
        if #queue == 0 then break end
        table.sort(queue, function(a, b)
            if a.p == b.p then return a.i < b.i end
            return a.p < b.p
        end)

        for _, farm in ipairs(queue) do
            if not anyFarmActive() or justRespawned then break end
            if not HRP or not HRP.Parent then break end
            setNoclip(true)

            if farm.t == "herb" and herbActive then
                doOneHerb()

            elseif farm.t == "treasure" and treasureActive then
                doOneTreasure()

            elseif farm.t == "altar" and altarActive then
                doOneAltar()

            elseif farm.t == "mob" and mobActive then
                -- Kill all alive mobs of selected type, then yield back to queue
                local found = doOneMob()
                while found and mobActive and not justRespawned do
                    task.wait(0.2)
                    found = doOneMob()
                end
                if not found and tick() - lastBossNotify >= NOTIFY_COOLDOWN then
                    -- no notify needed for mob not found, just skip silently
                end

            elseif farm.t == "boss" and bossActive then
                -- Kill all alive bosses, then yield back to queue
                local found = doOneBoss()
                while found and bossActive and not justRespawned do
                    task.wait(0.2)
                    found = doOneBoss()
                end
                if not found and tick() - lastBossNotify >= NOTIFY_COOLDOWN then
                    Rayfield:Notify({Title="Boss Farm", Content=table.concat(selectedBosses, ", ").." not spawned", Duration=3, Image=4483362458}))
                    lastBossNotify = tick()
                end
            end

            task.wait(0.05)
        end

        task.wait(0.05)
    end

    multiFarmThread  = nil
    combatFarmThread = nil
    if Humanoid and Humanoid.Parent then
        Humanoid.PlatformStand = false
        Humanoid.AutoRotate = true
    end
    if not anyFarmActive() then
        stopFlyNoclip()
        task.wait(0.1)
        snapToGround()
    end
end

local function ensureUnifiedFarmRunning()
    if multiFarmThread then
        local s = coroutine.status(multiFarmThread)
        if s == "suspended" or s == "running" then return end
        multiFarmThread = nil
        combatFarmThread = nil
    end
    if combatFarmThread then
        local s = coroutine.status(combatFarmThread)
        if s == "suspended" or s == "running" then return end
        combatFarmThread = nil
    end
    multiFarmThread  = task.spawn(runUnifiedFarm)
    combatFarmThread = multiFarmThread
end

local function ensureMultiFarmRunning()  ensureUnifiedFarmRunning() end
local function ensureCombatFarmRunning() ensureUnifiedFarmRunning() end

-- ============================================================
-- MOB FARMING
-- ============================================================
local COMBAT_DISTANCE = 6

local function findBoss()
    local folder = workspace:FindFirstChild("Enemies")
    if not folder or not selectedBosses or #selectedBosses == 0 then return nil end
    local nearest, shortest = nil, math.huge
    for _, mob in pairs(folder:GetChildren()) do
        for _, bName in ipairs(selectedBosses) do
            if mob.Name == bName then
                local hum = mob:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then
                    local ok, pos = pcall(function() return mob:GetPivot().Position end)
                    if ok then
                        local d = (HRP.Position - pos).Magnitude
                        if d < shortest then shortest = d; nearest = mob end
                    end
                end
                break
            end
        end
    end
    return nearest
end

local function farmBoss()
    if not selectedBosses or #selectedBosses == 0 then return end
    setNoclip(true)
    local fallbackActive = false  -- track sama ada tengah farm fallback atau tidak

    while bossActive do
        if justRespawned then
            while justRespawned and bossActive do task.wait(0.5) end
            if not bossActive then break end
            setNoclip(true)
        end
        if not bossActive then break end

        local nearest = findBoss()

        if nearest then
            -- Boss ada — stop fallback farm kalau tengah jalan
            if fallbackActive then
                fallbackActive = false
                -- Noclip kekal on, tak perlu stopFlyNoclip
                setNoclip(true)
            end

            local mobRoot = nearest:FindFirstChild("HumanoidRootPart")
            if not mobRoot then task.wait(0.5); continue end

            flyTo(getCombatCFrame(mobRoot).Position, function()
                return bossActive and findBoss() ~= nil
            end)
            task.wait(0.05)
            if not bossActive then break end
            if not nearest.Parent then task.wait(0.3); continue end

            local hum = nearest:FindFirstChildOfClass("Humanoid")
            if not hum or hum.Health <= 0 then task.wait(0.3); continue end

            Humanoid.PlatformStand = true
            while bossActive and nearest.Parent do
                if justRespawned then break end
                -- Semak boss lain spawn — kalau ada yang lagi dekat, break untuk retarget
                local newNearest = findBoss()
                if not newNearest or not newNearest.Parent then break end
                local h = newNearest:FindFirstChildOfClass("Humanoid")
                if not h or h.Health <= 0 then break end
                -- Guna nearest yang asal kalau masih alive
                local h2 = nearest:FindFirstChildOfClass("Humanoid")
                if not h2 or h2.Health <= 0 then break end
                if mobRoot and mobRoot.Parent then
                    HRP.CFrame = getCombatCFrame(mobRoot)
                    HRP.AssemblyLinearVelocity  = Vector3.zero
                    HRP.AssemblyAngularVelocity = Vector3.zero
                end
                stopCultivatingIfNeeded()
                pcall(function() ReplicatedStorage.RemoteEvents.Attack:FireServer("Light", {["RootPart"]=HRP}) end)
                task.wait(0.1)
            end
            if Humanoid and Humanoid.Parent then
                Humanoid.PlatformStand = false
                Humanoid.AutoRotate = true
            end
            if HRP and HRP.Parent then
                HRP.AssemblyLinearVelocity  = Vector3.zero
                HRP.AssemblyAngularVelocity = Vector3.zero
            end
            task.wait(0.3)

        else
            if tick() - lastBossNotify >= NOTIFY_COOLDOWN then
                Rayfield:Notify({Title="Boss Farm", Content=table.concat(selectedBosses,", ").." not spawned, farming others...", Duration=3, Image=4483362458}))
                lastBossNotify = tick()
            end

            local hasFallback = herbActive or treasureActive or altarActive
            if hasFallback then
                fallbackActive = true
                setNoclip(true)
                local queue = {}
                if altarActive    then table.insert(queue, {t="altar",    p=farmPriority.altar,    i=1}) end
                if treasureActive then table.insert(queue, {t="treasure", p=farmPriority.treasure, i=2}) end
                if herbActive     then table.insert(queue, {t="herb",     p=farmPriority.herb,     i=3}) end
                table.sort(queue, function(a,b) if a.p == b.p then return (a.i or 0) < (b.i or 0) end; return a.p < b.p end)
                local function noBoss() return findBoss() == nil end
                for _, farm in ipairs(queue) do
                    if not noBoss() then break end  -- boss spawn, keluar terus
                    setNoclip(true)
                    if farm.t == "herb" and herbActive then
                        doOneHerb(noBoss)
                    elseif farm.t == "treasure" and treasureActive then
                        doOneTreasure(noBoss)
                    elseif farm.t == "altar" and altarActive then
                        doOneAltar(noBoss)
                    end
                    if noBoss() then task.wait(0.3) end
                end

            else
                task.wait(3)
            end
        end
    end

    if Humanoid and Humanoid.Parent then
        Humanoid.PlatformStand = false
        Humanoid.AutoRotate = true
    end
    stopFlyNoclip()
    task.wait(0.1)
    snapToGround()
end



-- ============================================================
-- QUEST FUNCTIONS (ORIGINAL LOGIC)
-- ============================================================
local function getProgressFromServer(npcName)
    local function check(qd)
        if qd and qd:FindFirstChild("NpcName") and qd.NpcName.Value==npcName then
            if qd:FindFirstChild("Progress") and qd:FindFirstChild("ProgressEnd") then
                return qd.Progress.Value,qd.ProgressEnd.Value
            end
        end
        return nil,nil
    end
    local p,e=check(Player:FindFirstChild("QuestData")); if p then return p,e end
    return check(Player:FindFirstChild("SecondQuestData"))
end

local function acceptQuest(npcName)
    pcall(function() ReplicatedStorage.RemoteFunctions.Quest:InvokeServer(npcName,true) end)
    task.wait(0.5)
end

local function getActiveQuestOfType(qtype)
    for npcName, info in pairs(QUEST_DATA) do
        if info.Type == qtype then
            local prog, pEnd = getProgressFromServer(npcName)
            if prog and prog < pEnd then
                return npcName, prog, pEnd
            end
        end
    end
    return nil, nil, nil
end

local function farmQuestSlaying(npcName, target, lbl)
    local folder = workspace:FindFirstChild("Enemies")
    if not folder then return end
    setNoclip(true)
    while slayingActive do
        if justRespawned then while justRespawned and slayingActive do task.wait(0.5) end end
        if not slayingActive then break end
        local prog, pEnd = getProgressFromServer(npcName)
        if not prog then break end
        pcall(function() lbl:Set(prog .. " / " .. pEnd) end)
        if prog >= pEnd then break end
        local nearest, shortest = nil, math.huge
        for _, mob in pairs(folder:GetChildren()) do
            if mob.Name == target then
                local hum = mob:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then
                    local ok, pos = pcall(function() return mob:GetPivot().Position end)
                    if ok then
                        local d = (HRP.Position - pos).Magnitude
                        if d < shortest then shortest = d; nearest = mob end
                    end
                end
            end
        end
        if not nearest then
            setNoclip(false); snapToGround()
            local waited = 0
            while slayingActive and waited < 30 do
                task.wait(0.5); waited += 0.5
                local found = false
                for _, mob in pairs(folder:GetChildren()) do
                    if mob.Name == target then
                        local hum = mob:FindFirstChildOfClass("Humanoid")
                        if hum and hum.Health > 0 then found = true; break end
                    end
                end
                if found then break end
            end
            if slayingActive then setNoclip(true) end
            continue
        end
        local mobRoot = nearest:FindFirstChild("HumanoidRootPart")
        if not mobRoot then task.wait(0.5); continue end
        flyTo(getCombatCFrame(mobRoot).Position, function() return slayingActive end)
        task.wait(0.05)
        if not slayingActive then break end
        if not nearest.Parent then task.wait(0.5); continue end
        local hum = nearest:FindFirstChildOfClass("Humanoid")
        if not hum or hum.Health <= 0 then task.wait(0.5); continue end
        Humanoid.PlatformStand = true
        while slayingActive and nearest.Parent do
            if justRespawned then break end
            local h = nearest:FindFirstChildOfClass("Humanoid")
            if not h or h.Health <= 0 then break end
            if mobRoot and mobRoot.Parent then
                HRP.CFrame = getCombatCFrame(mobRoot)
                HRP.AssemblyLinearVelocity  = Vector3.zero
                HRP.AssemblyAngularVelocity = Vector3.zero
            end
            stopCultivatingIfNeeded()
            pcall(function() ReplicatedStorage.RemoteEvents.Attack:FireServer("Light", {["RootPart"]=HRP}) end)
            task.wait(0.1)
        end
        if Humanoid and Humanoid.Parent then
            Humanoid.PlatformStand = false
            Humanoid.AutoRotate = true
        end
        if HRP and HRP.Parent then
            HRP.Anchored = true
            HRP.AssemblyLinearVelocity  = Vector3.zero
            HRP.AssemblyAngularVelocity = Vector3.zero
        end
        setNoclip(true)
        task.wait(0.3)
    end
    if Humanoid and Humanoid.Parent then
        Humanoid.PlatformStand = false
        Humanoid.AutoRotate = true
    end
    if HRP and HRP.Parent then HRP.Anchored = false end
    stopFlyNoclip()
    task.wait(0.1)
    snapToGround()
end

local function farmQuestHarvesting(npcName, target, lbl)
    setNoclip(true)
    while harvestingActive do
        local prog, pEnd = getProgressFromServer(npcName)
        if not prog then break end
        pcall(function() lbl:Set(prog .. " / " .. pEnd) end)
        if prog >= pEnd then break end
        local herbFolder = getHerbFolder(target)
        if not herbFolder then task.wait(3); continue end
        local foundHerb = findRandomHerb(herbFolder, 0, nil)
        if not foundHerb or not foundHerb.Parent then task.wait(2); continue end
        local zone = foundHerb:FindFirstChild("HerbZone", true)
        if not zone then continue end
        local targetCF = getCF(zone)
        if not targetCF then task.wait(0.2); continue end
        local zonePos = targetCF.Position
        local safePos = Vector3.new(zonePos.X, zonePos.Y + 1.5, zonePos.Z)
        local reached = flyTo(safePos, function() return harvestingActive end)
        if not harvestingActive then break end
        setNoclip(true)
        if not reached then
            local zCF = getCF(zone)
            if zCF and HRP and HRP.Parent then
                HRP.CFrame = CFrame.new(zCF.Position.X, zCF.Position.Y + 1.5, zCF.Position.Z)
                HRP.AssemblyLinearVelocity = Vector3.zero
                task.wait(0.1)
            end
        end
        local attempt = 0
        while not isPlayerInZone(zone) and attempt < 20 and harvestingActive do
            attempt += 1
            local zCF = getCF(zone)
            if zCF then
                HRP.CFrame = CFrame.new(zCF.Position.X, zCF.Position.Y + 1.5, zCF.Position.Z)
                HRP.AssemblyLinearVelocity = Vector3.zero
            end
            task.wait(0.05)
        end
        if not isPlayerInZone(zone) then continue end
        task.wait(0.2)
        if not isPlayerInZone(zone) or not harvestingActive then continue end
        if HRP then HRP.Anchored = true end
        setNoclip(true)
        pcall(function() ReplicatedStorage.RemoteEvents.Harvest:FireServer() end)
        local result = waitForHarvestComplete(function() return harvestingActive end)
        if HRP then
            HRP.Anchored = false
            HRP.AssemblyLinearVelocity = Vector3.zero
        end
        if not harvestingActive then break end
        if result == "timeout" or result == "stuck" then
            task.wait(0.2)
        end
    end
end

-- ============================================================
-- ALCHEMY
-- ============================================================
local function runAlchemy(lblStatus,isLoop)
    if not selectedRecipe then
        Rayfield:Notify({Title="Alchemy",Content="Select a recipe first!",Duration=3, Image=4483362458}))
        if not isLoop then alchemyActive=false end; return
    end
    local recipe=ALCHEMY_RECIPES[selectedRecipe]; if not recipe then if not isLoop then alchemyActive=false end; return end
    local crafted=0; local target=alchemyCraftAmount
    pcall(function() lblStatus:Set(" Crafting: 0 / "..target) end)
    local VALID_AMOUNTS = {1, 2, 5, 25, 100, 1000}
    local function getLargestValidBatch(remaining)
        local best = 1
        for _, v in ipairs(VALID_AMOUNTS) do
            if v <= remaining then best = v end
        end
        return best
    end
    local subBatch = target >= 25 and 5 or getLargestValidBatch(target)
    while alchemyActive and crafted < target do
        local remaining = target - crafted
        local batch = (remaining >= subBatch) and subBatch or getLargestValidBatch(remaining)
        local herbArgs={}
        for i=1,4 do
            local element = recipe.Elements[i]
            local herbName = getBestHerbForElement(element)
            herbArgs[i] = {["Name"]=herbName, ["Variation"]=selectedAlchemyYear}
        end
        local ok, result = pcall(function()
            return ReplicatedStorage.RemoteFunctions.Concoct:InvokeServer(herbArgs, batch)
        end)
        if ok and type(result)=="table" then
            if result.Success then
                crafted += batch
                pcall(function() lblStatus:Set(" Crafting: "..crafted.." / "..target) end)
                local itemName = result.ItemName or selectedRecipe
                if itemName == "Failed Pill" then
                    Rayfield:Notify({Title="Alchemy",Content="Failed Pill — potency low or recipe not unlocked.",Duration=4, Image=4483362458}))
                else
                    Rayfield:Notify({Title="Alchemy",Content=itemName.." x"..batch.." ("..crafted.."/"..target..")",Duration=2, Image=4483362458}))
                end
            else
                local msg = result.Message or "Unknown error"
                Rayfield:Notify({Title="Alchemy Failed",Content=msg,Duration=4, Image=4483362458}))
                if msg:find("Not enough") or msg:find("Recipe does not exist") then
                    alchemyActive=false; break
                end
                task.wait(1)
            end
        else
            Rayfield:Notify({Title="Alchemy",Content="Server error. Retrying...",Duration=3, Image=4483362458}))
            task.wait(1)
        end
        task.wait(1.5)
    end
    if not isLoop then
        alchemyActive=false; pcall(function() lblStatus:Set(" Done! "..crafted.."x "..selectedRecipe) end)
        Rayfield:Notify({Title="Alchemy",Content="Done! "..crafted.."x "..selectedRecipe,Duration=4, Image=4483362458}))
    else
        pcall(function() lblStatus:Set(" Batch done: "..crafted.."x | Continuing...") end)
    end
end

-- ============================================================
-- SCAN WORKSPACE
-- ============================================================
local herbsList={}
if workspace:FindFirstChild("Herbs") then
    for _, h in pairs(workspace.Herbs:GetChildren()) do table.insert(herbsList,h.Name) end
end
if #herbsList==0 then herbsList={"Yin Bush","Yang Flower","Twilight Bloom","Taixian","Spirit Grass","Qilin Berries","Moonlight Flower","Lingzhi","Ginseng","Blood Flower"} end

local mobsList,mobsSet={},{}
if workspace:FindFirstChild("Enemies") then
    for _, m in pairs(workspace.Enemies:GetChildren()) do
        if not mobsSet[m.Name] then table.insert(mobsList,m.Name); mobsSet[m.Name]=true end
    end
end
if #mobsList==0 then mobsList={"No mobs found"} end

local trainingList,trainingData={},{}
if workspace:FindFirstChild("Training Zones") then
    for _, sub in pairs({"Training","Qi"}) do
        local s=workspace["Training Zones"]:FindFirstChild(sub)
        if s then for _, z in pairs(s:GetChildren()) do local n=sub.." - "..z.Name; table.insert(trainingList,n); trainingData[n]=z end end
    end
end

-- ============================================================
-- HERB ESP
-- ============================================================
local espActive=false; local selectedESPHerbs={}; local espObjects={}
local espMaxDistance=5000; local espShowHighlight=true; local espShowText=true; local espYearFilter=nil

local function createHerbESP(herb,herbName)
    local herbZone=herb:FindFirstChild("HerbZone",true)
    if not herbZone or not herbZone:IsA("BasePart") then return end
    local variation=getHerbVariation(herb)
    if espYearFilter and variation~=espYearFilter then return end
    local espColor=YEAR_COLOURS[variation] or Color3.fromRGB(0,255,0)
    local data={herb=herb,herbZone=herbZone,herbName=herbName,variation=variation}
    if espShowText then
        local bb=Instance.new("BillboardGui"); bb.Name="HerbESP"; bb.Adornee=herbZone
        bb.Size=UDim2.new(0,160,0,70); bb.StudsOffset=Vector3.new(0,4,0); bb.AlwaysOnTop=true; bb.Parent=herbZone
        local lbl=Instance.new("TextLabel"); lbl.Size=UDim2.new(1,0,1,0); lbl.BackgroundTransparency=1
        lbl.TextColor3=espColor; lbl.TextStrokeTransparency=0; lbl.TextStrokeColor3=Color3.fromRGB(0,0,0)
        lbl.TextScaled=true; lbl.Font=Enum.Font.GothamBold; lbl.Parent=bb
        data.billboard=bb; data.label=lbl
    end
    if espShowHighlight then
        local hl=Instance.new("Highlight"); hl.FillColor=espColor; hl.OutlineColor=Color3.fromRGB(255,255,255)
        hl.FillTransparency=0.6; hl.OutlineTransparency=0.3; hl.DepthMode=Enum.HighlightDepthMode.AlwaysOnTop; hl.Parent=herbZone
        data.highlight=hl
    end
    table.insert(espObjects,data)
end

local function updateHerbESP()
    if not espActive then return end
    local char=Player.Character; if not char then return end
    local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
    for i=#espObjects,1,-1 do
        local d=espObjects[i]
        if d.herb and d.herb.Parent and d.herbZone and d.herbZone.Parent then
            local dist=(hrp.Position-d.herbZone.Position).Magnitude; local visible=dist<=espMaxDistance
            if d.billboard then d.billboard.Enabled=visible end
            if d.highlight then d.highlight.Enabled=visible end
            if visible and d.label then d.label.Text=string.format("%s\n%.0f studs",d.herbName,dist) end
        else
            if d.billboard then d.billboard:Destroy() end; if d.highlight then d.highlight:Destroy() end; table.remove(espObjects,i)
        end
    end
end

local function clearHerbESP()
    for _, d in pairs(espObjects) do if d.billboard then d.billboard:Destroy() end; if d.highlight then d.highlight:Destroy() end end; espObjects={}
end

local function refreshHerbESP()
    clearHerbESP(); if not espActive then return end
    local herbs=workspace:FindFirstChild("Herbs"); if not herbs then return end
    for herbName,enabled in pairs(selectedESPHerbs) do
        if enabled then
            local hf=herbs:FindFirstChild(herbName)
            if hf then local folder=hf:FindFirstChild("Folder") or hf; for _, herb in pairs(folder:GetChildren()) do createHerbESP(herb,herbName) end end
        end
    end
end

RunService.RenderStepped:Connect(function() if espActive then updateHerbESP() end end)

-- ============================================================
-- TABS
-- ============================================================

-- TAB 1 HERB
local CONFIG_FILE = "MelayuHub_config.json"

local function encodeConfig()
    local cfg = {
        -- Selections
        selectedHerb           = selectedHerb,
        selectedMob            = selectedMob,
        selectedTreasure       = selectedTreasure,
        selectedBloodline      = selectedBloodline,
        selectedBosses         = selectedBosses,
        selectedSlayingQuest   = selectedSlayingQuest,
        selectedHarvestingQuest= selectedHarvestingQuest,
        selectedRecipe         = selectedRecipe,
        selectedAlchemyYear    = selectedAlchemyYear,
        alchemyCraftAmount     = alchemyCraftAmount,
        selectedAutoThreshold  = selectedAutoThreshold,
        autoPillSlot           = autoPillSlot,
        -- Element herbs
        selectedElementHerb    = selectedElementHerb,
        -- Priority
        farmPriority           = farmPriority,
        -- Settings
        flySpeed               = flySpeed,
        mobDistance            = mobDistance,
        bossNotifyEnabled      = bossNotifyEnabled,
        -- Quest loops
        questLoopSlaying       = questLoopSlaying,
        questLoopHarvesting    = questLoopHarvesting,
    }
    -- Simple JSON encode
    local function encVal(v)
        local t = type(v)
        if t == "nil" then return "null"
        elseif t == "boolean" then return v and "true" or "false"
        elseif t == "number" then return tostring(v)
        elseif t == "string" then return '"' .. v:gsub('"', '\"') .. '"'
        elseif t == "table" then
            -- Check if array
            local isArr = true
            local n = 0
            for k in pairs(v) do n += 1; if type(k) ~= "number" then isArr = false; break end end
            if isArr then
                local parts = {}
                for _, val in ipairs(v) do table.insert(parts, encVal(val)) end
                return "[" .. table.concat(parts, ",") .. "]"
            else
                local parts = {}
                for k, val in pairs(v) do
                    table.insert(parts, '"' .. tostring(k) .. '":' .. encVal(val))
                end
                return "{" .. table.concat(parts, ",") .. "}"
            end
        end
        return "null"
    end
    return encVal(cfg)
end

local function saveConfig()
    pcall(function()
        writefile(CONFIG_FILE, encodeConfig())
    end)
end

local function loadConfig()
    local ok, data = pcall(function() return readfile(CONFIG_FILE) end)
    if not ok or not data or data == "" then return end

    local function decodeSimple(s)
        s = s:match("^%s*(.-)%s*$")
        if s == "null" then return nil
        elseif s == "true" then return true
        elseif s == "false" then return false
        elseif s:match('^"(.*)"$') then
            return s:match('^"(.*)"$'):gsub('\"', '"')
        elseif tonumber(s) then return tonumber(s)
        end
        return nil
    end

    local function decodeArray(s)
        local arr = {}
        s = s:match("^%[(.*)%]$")
        if not s then return arr end
        local depth2, cur, inStr = 0, "", false
        for idx = 1, #s do
            local c = s:sub(idx, idx)
            if c == '"' then inStr = not inStr end
            if not inStr then
                if c == "{" or c == "[" then depth2 += 1
                elseif c == "}" or c == "]" then depth2 -= 1
                elseif c == "," and depth2 == 0 then
                    local v = decodeSimple(cur:match("^%s*(.-)%s*$"))
                    if v ~= nil then table.insert(arr, v) end
                    cur = ""; continue
                end
            end
            cur = cur .. c
        end
        if cur ~= "" then
            local v = decodeSimple(cur:match("^%s*(.-)%s*$"))
            if v ~= nil then table.insert(arr, v) end
        end
        return arr
    end

    local function getField(json, key)
        local val = json:match('"' .. key .. '"%s*:%s*("(?:[^"\\]|\\.)*"|true|false|null|%-?%d+%.?%d*)')
        if val then return decodeSimple(val) end
        local arr = json:match('"' .. key .. '"%s*:%s*(%[.-%])')
        if arr then return decodeArray(arr) end
        local obj = json:match('"' .. key .. '"%s*:%s*({[^}]*})')
        if obj then
            local result = {}
            for k, v in obj:gmatch('"([^"]+)"%s*:%s*("(?:[^"\\]|\\.)*"|true|false|null|%-?%d+%.?%d*)') do
                result[k] = decodeSimple(v)
            end
            return result
        end
        return nil
    end

    local h = getField(data, "selectedHerb")
    if type(h) == "table" and #h > 0 then selectedHerb = h
    elseif type(h) == "string" then selectedHerb = {h} end

    local mob = getField(data, "selectedMob")
    if mob then selectedMob = mob end

    local tr = getField(data, "selectedTreasure")
    if type(tr) == "table" then selectedTreasure = tr end

    local bl = getField(data, "selectedBloodline")
    if type(bl) == "table" then selectedBloodline = bl end

    local bs = getField(data, "selectedBosses")
    if type(bs) == "table" and #bs > 0 then selectedBosses = bs end

    local sq = getField(data, "selectedSlayingQuest")
    if sq then selectedSlayingQuest = sq end

    local hq = getField(data, "selectedHarvestingQuest")
    if hq then selectedHarvestingQuest = hq end

    local rec = getField(data, "selectedRecipe")
    if rec then selectedRecipe = rec end

    local ay = getField(data, "selectedAlchemyYear")
    if ay then selectedAlchemyYear = ay end

    local ca = getField(data, "alchemyCraftAmount")
    if ca then alchemyCraftAmount = ca end

    local sat = getField(data, "selectedAutoThreshold")
    if sat then selectedAutoThreshold = sat end

    local aps = getField(data, "autoPillSlot")
    if aps then autoPillSlot = aps end

    local seh = getField(data, "selectedElementHerb")
    if type(seh) == "table" then
        for k, v in pairs(seh) do selectedElementHerb[k] = v end
    end

    local fp = getField(data, "farmPriority")
    if type(fp) == "table" then
        for k, v in pairs(fp) do if farmPriority[k] then farmPriority[k] = v end end
    end

    local fs = getField(data, "flySpeed")
    if fs then flySpeed = fs end

    local md = getField(data, "mobDistance")
    if md then mobDistance = md end

    local bn = getField(data, "bossNotifyEnabled")
    if bn ~= nil then bossNotifyEnabled = bn end

    local qls = getField(data, "questLoopSlaying")
    if qls ~= nil then questLoopSlaying = qls end

    local qlh = getField(data, "questLoopHarvesting")
    if qlh ~= nil then questLoopHarvesting = qlh end
end


local Tab1 = Window:CreateTab(" Herb", "zap")
Tab1:CreateSection("Herb Farm")
Tab1:CreateDropdown({Name="Select Herb(s)",Flag="HerbSelect",Options=herbsList,CurrentOption="",
    Callback=function(opts)
        if type(opts) == "string" then
            -- NexusLib sends a single string per click — manually accumulate
            if type(selectedHerb) ~= "table" then selectedHerb = {} end
            local found = false
            for i, v in ipairs(selectedHerb) do
                if v == opts then table.remove(selectedHerb, i); found = true; break end
            end
            if not found then table.insert(selectedHerb, opts) end
        else
            selectedHerb = opts
        end
        herbZoneIndices = {}
        herbRoundRobinIndex = 1
        saveConfig()
    end})
Tab1:CreateDropdown({Name="Priority (1=first, 5=last)",Flag="HerbPriority",Options={"1","2","3","4","5"},CurrentOption="3",
    Callback=function(opt[1])
        local newP = tonumber(opt[1]) or 3
        for k, v in pairs(farmPriority) do
            if k ~= "herb" and v == newP then
                farmPriority[k] = farmPriority.herb
                break
            end
        end
        farmPriority.herb = newP
        saveConfig()
        Rayfield:Notify({Title="Herb Farm",Content="Priority set to "..opt[1],Duration=2, Image=4483362458}))
    end})
Tab1:CreateToggle({Name="Auto Farm Herb",CurrentValue=false,
    Callback=function(val)
        if val then
            if not selectedHerb or (type(selectedHerb)=="table" and #selectedHerb==0) then
                Rayfield:Notify({Title="Error",Content="Select herb(s) first!",Duration=3, Image=4483362458})); return
            end
            herbActive=true
            ensureUnifiedFarmRunning()
            Rayfield:Notify({Title="Herb Farm",Content="Started!",Duration=3, Image=4483362458}))
        else
            herbActive=false
            if not anyFarmActive() then
                if multiFarmThread then pcall(function() task.cancel(multiFarmThread) end); multiFarmThread=nil; combatFarmThread=nil end
                stopFlyNoclip(); task.wait(0.5); snapToGround()
            end
            Rayfield:Notify({Title="Herb Farm",Content="Stopped.",Duration=2, Image=4483362458}))
        end
    end})

Tab1:CreateSection("Herb ESP")
Tab1:CreateDropdown({Name="Herbs for ESP",Options=herbsList,CurrentOption="",
    Callback=function(sel) selectedESPHerbs={}; for _, n in pairs(sel) do selectedESPHerbs[n]=true end; if espActive then refreshHerbESP() end end})
Tab1:CreateToggle({Name="Enable Herb ESP",CurrentValue=false,
    Callback=function(val) espActive=val; if val then refreshHerbESP() else clearHerbESP() end end})
Tab1:CreateSlider({Name="Max Distance",Flag="ESPMaxDist",Range={100,5000},Increment=100,CurrentValue=5000,Callback=function(val) espMaxDistance=val end})
Tab1:CreateToggle({Name="Show Highlight",Flag="ESPHighlight",CurrentValue=true,Callback=function(val) espShowHighlight=val; if espActive then refreshHerbESP() end end})
Tab1:CreateToggle({Name="Show Text",Flag="ESPText",CurrentValue=true,Callback=function(val) espShowText=val; if espActive then refreshHerbESP() end end})
Tab1:CreateButton({Name=" Refresh ESP",Callback=function() refreshHerbESP(); Rayfield:Notify({Title="ESP",Content=#espObjects.." herbs",Duration=2, Image=4483362458})) end})

-- TAB 2 TREASURE
local Tab2 = Window:CreateTab(" Treasure", "zap")
Tab2:CreateSection("Treasure Farm")
Tab2:CreateDropdown({Name="Treasure Category",Flag="TreasureSelect",Options=TREASURE_CATEGORIES,CurrentOption="All Treasures",
    Callback=function(opts)
        selectedTreasure = {}
        for _, pick in ipairs(opts) do table.insert(selectedTreasure, pick) end
        local label = #selectedTreasure > 0 and table.concat(selectedTreasure, ", ") or "All"
        saveConfig()
        Rayfield:Notify({Title="Treasure",Content="Selected: "..label,Duration=2, Image=4483362458}))
    end})
Tab2:CreateDropdown({Name="Priority (1=first, 5=last)",Flag="TreasurePriority",Options={"1","2","3","4","5"},CurrentOption="2",
    Callback=function(opt[1])
        local newP = tonumber(opt[1]) or 2
        for k, v in pairs(farmPriority) do
            if k ~= "treasure" and v == newP then
                farmPriority[k] = farmPriority.treasure
                break
            end
        end
        farmPriority.treasure = newP
        saveConfig()
        Rayfield:Notify({Title="Treasure Farm",Content="Priority set to "..opt[1],Duration=2, Image=4483362458}))
    end})
Tab2:CreateToggle({Name="Auto Farm Treasure",CurrentValue=false,
    Callback=function(val)
        if val then
            treasureActive=true
            ensureUnifiedFarmRunning()
            local label = #selectedTreasure > 0 and table.concat(selectedTreasure, ", ") or "All"
            Rayfield:Notify({Title="Treasure Farm",Content="Farming: "..label,Duration=3, Image=4483362458}))
        else
            treasureActive=false
            if not anyFarmActive() then
                if multiFarmThread then pcall(function() task.cancel(multiFarmThread) end); multiFarmThread=nil; combatFarmThread=nil end
                stopFlyNoclip(); task.wait(0.5); snapToGround()
            end
            Rayfield:Notify({Title="Treasure Farm",Content="Stopped.",Duration=2, Image=4483362458}))
        end
    end})

-- TAB 3 ALTAR
local Tab3 = Window:CreateTab(" Altar", "zap")
Tab3:CreateSection("Altar Farm")
Tab3:CreateDropdown({Name="Priority (1=first, 5=last)",Flag="AltarPriority",Options={"1","2","3","4","5"},CurrentOption="1",
    Callback=function(opt[1])
        local newP = tonumber(opt[1]) or 1
        for k, v in pairs(farmPriority) do
            if k ~= "altar" and v == newP then
                farmPriority[k] = farmPriority.altar
                break
            end
        end
        farmPriority.altar = newP
        saveConfig()
        Rayfield:Notify({Title="Altar Farm",Content="Priority set to "..opt[1],Duration=2, Image=4483362458}))
    end})
Tab3:CreateToggle({Name="Auto Collect Altar",CurrentValue=false,
    Callback=function(val)
        if val then
            altarActive=true
            ensureUnifiedFarmRunning()
            Rayfield:Notify({Title="Altar Farm",Content="Started.",Duration=3, Image=4483362458}))
        else
            altarActive=false
            if not anyFarmActive() then
                if multiFarmThread then pcall(function() task.cancel(multiFarmThread) end); multiFarmThread=nil; combatFarmThread=nil end
                stopFlyNoclip(); task.wait(0.5); snapToGround()
            end
            Rayfield:Notify({Title="Altar Farm",Content="Stopped.",Duration=2, Image=4483362458}))
        end
    end})

-- TAB 4 MOB
local Tab4 = Window:CreateTab(" Mob", "zap")
Tab4:CreateSection("Combat Settings")
Tab4:CreateDropdown({Name="Select Mob",Flag="MobSelect",Options=mobsList,CurrentOption="Select",
    Callback=function(opt[1]) selectedMob=opt[1]
    saveConfig()
end})
Tab4:CreateSlider({Name="Distance (studs)",Flag="MobDistance",Range={3,15},Increment=1,CurrentValue=6,
    Callback=function(val) mobDistance=val; saveConfig() end})
Tab4:CreateDropdown({Name="Priority (1=first, 5=last)",Flag="MobPriority",Options={"1","2","3","4","5"},CurrentOption="4",
    Callback=function(opt[1])
        local newP = tonumber(opt[1]) or 4
        for k, v in pairs(farmPriority) do
            if k ~= "mob" and v == newP then
                farmPriority[k] = farmPriority.mob
                break
            end
        end
        farmPriority.mob = newP
        saveConfig()
        Rayfield:Notify({Title="Mob Farm",Content="Priority set to "..opt[1],Duration=2, Image=4483362458}))
    end})
Tab4:CreateToggle({Name="Auto Farm Mob",CurrentValue=false,
    Callback=function(val)
        if val then
            if not selectedMob or selectedMob=="No mobs found" then
                Rayfield:Notify({Title="Error",Content="Select mob!",Duration=3, Image=4483362458})); return
            end
            mobActive=true
            ensureUnifiedFarmRunning()
            Rayfield:Notify({Title="Mob Farm",Content="Started! Priority: "..farmPriority.mob,Duration=3, Image=4483362458}))
        else
            mobActive=false
            if not anyFarmActive() then
                if multiFarmThread then pcall(function() task.cancel(multiFarmThread) end); multiFarmThread=nil; combatFarmThread=nil end
                stopFlyNoclip(); task.wait(0.5); snapToGround()
            end
            Rayfield:Notify({Title="Mob Farm",Content="Stopped.",Duration=2, Image=4483362458}))
        end
    end})

-- TAB 4B BOSS FARM
local TabBoss = Window:CreateTab(" Boss Farm", "zap")
TabBoss:CreateSection("Boss Combat")
TabBoss:CreateLabel("Distance follows Mob Farm slider.")
TabBoss:CreateDropdown({
    Name = "Select Boss", Flag = "BossSelect", Options = BOSS_MOBS,
    CurrentOption = "",
    Callback = function(opt[1])
        if opt[1] and opt[1] ~= "" then selectedBosses = {opt[1]} else selectedBosses = {} end
        saveConfig()
    end})
TabBoss:CreateDropdown({Name="Priority (1=first, 5=last)",Flag="BossPriority",Options={"1","2","3","4","5"},CurrentOption="5",
    Callback=function(opt[1])
        local newP = tonumber(opt[1]) or 5
        for k, v in pairs(farmPriority) do
            if k ~= "boss" and v == newP then
                farmPriority[k] = farmPriority.boss
                break
            end
        end
        farmPriority.boss = newP
        saveConfig()
        Rayfield:Notify({Title="Boss Farm",Content="Priority set to "..opt[1],Duration=2, Image=4483362458}))
    end})
TabBoss:CreateToggle({Name="Auto Farm Boss", CurrentValue=false,
    Callback=function(val)
        if val then
            if not selectedBosses or #selectedBosses == 0 then
                Rayfield:Notify({Title="Boss Farm", Content="Select at least one boss!", Duration=3, Image=4483362458})); return
            end
            bossActive = true
            ensureUnifiedFarmRunning()
            Rayfield:Notify({Title="Boss Farm", Content="Farming: "..table.concat(selectedBosses, ", ").." | Priority: "..farmPriority.boss, Duration=3, Image=4483362458}))
        else
            bossActive = false
            if not anyFarmActive() then
                if multiFarmThread then pcall(function() task.cancel(multiFarmThread) end); multiFarmThread=nil; combatFarmThread=nil end
                stopFlyNoclip(); task.wait(0.5); snapToGround()
            end
            Rayfield:Notify({Title="Boss Farm", Content="Stopped.", Duration=2, Image=4483362458}))
        end
    end})

-- TAB 5 QUEST
local Tab5 = Window:CreateTab(" Quest", "zap")

local selectedSlayingQuest    = nil
local selectedHarvestingQuest = nil

local function questPathWarning(questName)
    local info = QUEST_DATA[questName]
    if info and info.Path then
        local msg = info.Path == "Demon"
            and "Warning: This quest is for Demon path only!"
            or  "Warning: This quest is for " .. info.Path .. " path only!"
        Rayfield:Notify({Title="Quest Warning",Content=msg,Duration=6, Image=4483362458}))
    end
end

local function runHarvestingCycle(progLbl)
    local activeName, activeProg, activePEnd = getActiveQuestOfType("Harvesting")
    if activeName and activeName ~= selectedHarvestingQuest then
        local aInfo = QUEST_DATA[activeName]
        if aInfo then
            pcall(function() progLbl:Set(activeProg .. " / " .. activePEnd) end)
            farmQuestHarvesting(activeName, aInfo.Target, progLbl)
        end
        if not harvestingActive then return end
    end
    if not selectedHarvestingQuest then return end
    local info = QUEST_DATA[selectedHarvestingQuest]
    if not info then return end
    local prog, pEnd = getProgressFromServer(selectedHarvestingQuest)
    if not prog then
        acceptQuest(selectedHarvestingQuest); task.wait(1)
        prog, pEnd = getProgressFromServer(selectedHarvestingQuest)
    end
    if not prog then return end
    if prog >= pEnd then return end
    pcall(function() progLbl:Set(prog .. " / " .. pEnd) end)
    farmQuestHarvesting(selectedHarvestingQuest, info.Target, progLbl)
end

local function runSlayingCycle(progLbl)
    local activeName, activeProg, activePEnd = getActiveQuestOfType("Slaying")
    if activeName and activeName ~= selectedSlayingQuest then
        local aInfo = QUEST_DATA[activeName]
        if aInfo then
            pcall(function() progLbl:Set(activeProg .. " / " .. activePEnd) end)
            farmQuestSlaying(activeName, aInfo.Target, progLbl)
        end
        if not slayingActive then return end
    end
    if not selectedSlayingQuest then return end
    local info = QUEST_DATA[selectedSlayingQuest]
    if not info then return end
    local prog, pEnd = getProgressFromServer(selectedSlayingQuest)
    if not prog then
        acceptQuest(selectedSlayingQuest); task.wait(1)
        prog, pEnd = getProgressFromServer(selectedSlayingQuest)
    end
    if not prog then return end
    if prog >= pEnd then return end
    pcall(function() progLbl:Set(prog .. " / " .. pEnd) end)
    farmQuestSlaying(selectedSlayingQuest, info.Target, progLbl)
end

-- HARVESTING QUESTS
Tab5:CreateSection("Harvesting Quests")
local hqLblNPC  = Tab5:CreateLabel("NPC: -")
local hqLblTgt  = Tab5:CreateLabel("Target: -")
hqLblProg = Tab5:CreateLabel("Progress: -")

Tab5:CreateDropdown({
    Name = "Select Harvesting Quest", Flag = "HarvestingQuest", Options = harvestingQuestNames,
    CurrentOption="Select",
    Callback = function(opt[1])
        selectedHarvestingQuest = opt[1]
        local info = QUEST_DATA[selectedHarvestingQuest]
        if info then
            pcall(function() pcall(function() hqLblNPC:Set("NPC: " .. selectedHarvestingQuest) end)
            pcall(function() pcall(function() hqLblTgt:Set("Target: " .. info.Target .. " x" .. info.Amount) end)
            pcall(function() pcall(function() hqLblProg:Set("Progress: -") end)
            questPathWarning(selectedHarvestingQuest)
        end
        saveConfig()
    end})

Tab5:CreateToggle({Name = "Loop", Flag = "HarvestingLoop", CurrentValue = false,
    Callback = function(val) questLoopHarvesting = val end})

Tab5:CreateToggle({Name = "Auto Farm Harvesting", CurrentValue = false,
    Callback = function(val)
        if val then
            if not selectedHarvestingQuest then
                Rayfield:Notify({Title="Quest", Content="Select a harvesting quest first!", Duration=3, Image=4483362458})); return
            end
            harvestingActive = true
            harvestingFarmThread = task.spawn(function()
                while harvestingActive do
                    local activeName = getActiveQuestOfType("Harvesting")
                    if activeName and activeName ~= selectedHarvestingQuest then
                        local aInfo = QUEST_DATA[activeName]
                        if aInfo then
                            pcall(function() pcall(function() hqLblNPC:Set("NPC: " .. activeName) end)
                            pcall(function() pcall(function() hqLblTgt:Set("Target: " .. aInfo.Target .. " x" .. aInfo.Amount) end)
                        end
                    else
                        local info = QUEST_DATA[selectedHarvestingQuest]
                        if info then
                            pcall(function() pcall(function() hqLblNPC:Set("NPC: " .. selectedHarvestingQuest) end)
                            pcall(function() pcall(function() hqLblTgt:Set("Target: " .. info.Target .. " x" .. info.Amount) end)
                        end
                    end
                    runHarvestingCycle(hqLblProg)
                    if not harvestingActive then break end
                    local prog, pEnd = getProgressFromServer(selectedHarvestingQuest)
                    if not prog or prog >= (pEnd or 0) then
                        if questLoopHarvesting then
                            acceptQuest(selectedHarvestingQuest); task.wait(1)
                        else
                            harvestingActive = false; stopFlyNoclip(); break
                        end
                    end
                    task.wait(0.5)
                end
                stopFlyNoclip()
            end)
            Rayfield:Notify({Title="Quest", Content="Harvesting started", Duration=3, Image=4483362458}))
        else
            harvestingActive = false
            if harvestingFarmThread then pcall(function() task.cancel(harvestingFarmThread) end); harvestingFarmThread = nil end
            stopFlyNoclip(); task.wait(0.5); snapToGround()
            pcall(function() pcall(function() hqLblNPC:Set("NPC: -") end); pcall(function() pcall(function() hqLblTgt:Set("Target: -") end); pcall(function() pcall(function() hqLblProg:Set("Progress: -") end)
            Rayfield:Notify({Title="Quest", Content="Harvesting stopped.", Duration=2, Image=4483362458}))
        end
    end})

-- SLAYING QUESTS
Tab5:CreateSection("Slaying Quests")
local sqLblNPC  = Tab5:CreateLabel("NPC: -")
local sqLblTgt  = Tab5:CreateLabel("Target: -")
sqLblProg = Tab5:CreateLabel("Progress: -")

Tab5:CreateDropdown({
    Name = "Select Slaying Quest", Flag = "SlayingQuest", Options = slayingQuestNames,
    CurrentOption="Select",
    Callback = function(opt[1])
        selectedSlayingQuest = opt[1]
        local info = QUEST_DATA[selectedSlayingQuest]
        if info then
            pcall(function() pcall(function() sqLblNPC:Set("NPC: " .. selectedSlayingQuest) end)
            pcall(function() pcall(function() sqLblTgt:Set("Target: " .. info.Target .. " x" .. info.Amount) end)
            pcall(function() pcall(function() sqLblProg:Set("Progress: -") end)
            questPathWarning(selectedSlayingQuest)
        end
        saveConfig()
    end})

Tab5:CreateToggle({Name = "Loop", Flag = "SlayingLoop", CurrentValue = false,
    Callback = function(val) questLoopSlaying = val end})

Tab5:CreateToggle({Name = "Auto Farm Slaying", CurrentValue = false,
    Callback = function(val)
        if val then
            if not selectedSlayingQuest then
                Rayfield:Notify({Title="Quest", Content="Select a slaying quest first!", Duration=3, Image=4483362458})); return
            end
            slayingActive = true
            slayingFarmThread = task.spawn(function()
                while slayingActive do
                    local activeName = getActiveQuestOfType("Slaying")
                    if activeName and activeName ~= selectedSlayingQuest then
                        local aInfo = QUEST_DATA[activeName]
                        if aInfo then
                            pcall(function() pcall(function() sqLblNPC:Set("NPC: " .. activeName) end)
                            pcall(function() pcall(function() sqLblTgt:Set("Target: " .. aInfo.Target .. " x" .. aInfo.Amount) end)
                        end
                    else
                        local info = QUEST_DATA[selectedSlayingQuest]
                        if info then
                            pcall(function() pcall(function() sqLblNPC:Set("NPC: " .. selectedSlayingQuest) end)
                            pcall(function() pcall(function() sqLblTgt:Set("Target: " .. info.Target .. " x" .. info.Amount) end)
                        end
                    end
                    runSlayingCycle(sqLblProg)
                    if not slayingActive then break end
                    local prog, pEnd = getProgressFromServer(selectedSlayingQuest)
                    if not prog or prog >= (pEnd or 0) then
                        if questLoopSlaying then
                            acceptQuest(selectedSlayingQuest); task.wait(1)
                        else
                            slayingActive = false; stopFlyNoclip(); task.wait(0.5); snapToGround(); break
                        end
                    end
                    task.wait(0.5)
                end
                stopFlyNoclip()
            end)
            Rayfield:Notify({Title="Quest", Content="Slaying started", Duration=3, Image=4483362458}))
        else
            slayingActive = false
            if slayingFarmThread then pcall(function() task.cancel(slayingFarmThread) end); slayingFarmThread = nil end
            stopFlyNoclip(); task.wait(0.5); snapToGround()
            pcall(function() pcall(function() sqLblNPC:Set("NPC: -") end); pcall(function() pcall(function() sqLblTgt:Set("Target: -") end); pcall(function() pcall(function() sqLblProg:Set("Progress: -") end)
            Rayfield:Notify({Title="Quest", Content="Slaying stopped.", Duration=2, Image=4483362458}))
        end
    end})

-- TAB 6 TRAINING/NPC
local Tab6 = Window:CreateTab(" Training/NPC", "zap")
Tab6:CreateSection("Training Zones")
if #trainingList>0 then
    Tab6:CreateDropdown({Name="Training Zone",Options=trainingList,CurrentOption="Select",
        Callback=function(opt[1]) local zone=trainingData[opt[1]]; if zone then local cf=getCF(zone); if cf then flyTo(cf.Position,function() return true end) end end end})
else Tab6:CreateLabel("No training zones found.") end
Tab6:CreateSection("NPC Teleport")
local npcFolder=workspace:FindFirstChild("NPCs")
if npcFolder then
    local npcList={}; for _, npc in pairs(npcFolder:GetChildren()) do table.insert(npcList,npc.Name) end
    if #npcList>0 then
        Tab6:CreateDropdown({Name="Select NPC",Options=npcList,CurrentOption="Select",
            Callback=function(opt[1]) local npc=npcFolder:FindFirstChild(opt[1]); if npc then local cf=getCF(npc); if cf then flyTo(cf.Position,function() return true end) end end end})
    else Tab6:CreateLabel("No NPCs found.") end
else Tab6:CreateLabel("NPCs folder not found.") end

-- TAB 7 ALCHEMY
local Tab7 = Window:CreateTab(" Alchemy", "zap")
Tab7:CreateLabel("Make sure you already unlocked the recipe, have the right herb & year, and base value is not lower than required.")
local alchLblReq       = Tab7:CreateLabel(" Requirement: -")
local alchLblElem      = Tab7:CreateLabel(" Elements: -")
local alchLblHerbsUsed = Tab7:CreateLabel(" Herbs used: -")
local alchLblStatus    = Tab7:CreateLabel(" Status: Idle")
local alchLblBaseCheck = Tab7:CreateLabel(" Base Value: Select a recipe and year first.")

local function refreshAlchLbls()
    if not selectedRecipe or not ALCHEMY_RECIPES[selectedRecipe] then return end
    local r = ALCHEMY_RECIPES[selectedRecipe]
    local herbUsed = {}
    for _, element in ipairs(r.Elements) do
        table.insert(herbUsed, getBestHerbForElement(element))
    end
    pcall(function() alchLblHerbsUsed:Set(" Herbs: "..table.concat(herbUsed," | ") end))
    local total = calcRecipeBase(r, selectedAlchemyYear)
    if total < r.Base then
        pcall(function() alchLblBaseCheck:Set(string.format(" Base too low! Got ~%d, need %d",total,r.Base) end))
    else
        pcall(function() alchLblBaseCheck:Set(string.format(" Base OK: ~%d / %d required.",total,r.Base) end))
    end
end

Tab7:CreateSection("Recipe")
Tab7:CreateDropdown({Name="Select Recipe",Flag="AlchRecipe",Options=recipeNames,CurrentOption="Select",
    Callback=function(opt[1])
        selectedRecipe=opt[1]; local r=ALCHEMY_RECIPES[selectedRecipe]
        if r then
            pcall(function() alchLblReq:Set(" Requirement: "..r.Requirement) end)
            pcall(function() alchLblElem:Set(" Elements: "..table.concat(r.Elements," | ") end))
            refreshAlchLbls()
        end
    end})

Tab7:CreateSection("Select Herb per Element")
Tab7:CreateDropdown({Name="Earth herb",Flag="AlchEarth",Options={"Taixian","Ginseng"},CurrentOption="Taixian",
    Callback=function(opt[1]) selectedElementHerb["Earth"]=opt[1]; refreshAlchLbls() end})
Tab7:CreateDropdown({Name="Heaven herb",Flag="AlchHeaven",Options={"Moonlight Flower","Qilin Berries"},CurrentOption="Moonlight Flower",
    Callback=function(opt[1]) selectedElementHerb["Heaven"]=opt[1]; refreshAlchLbls() end})
Tab7:CreateDropdown({Name="Yin herb",Flag="AlchYin",Options={"Yin Bush","Blood Flower","Twilight Bloom","Spirit Grass"},CurrentOption="Yin Bush",
    Callback=function(opt[1]) selectedElementHerb["Yin"]=opt[1]; refreshAlchLbls() end})
Tab7:CreateDropdown({Name="Yang herb",Flag="AlchYang",Options={"Yang Flower","Lingzhi"},CurrentOption="Yang Flower",
    Callback=function(opt[1]) selectedElementHerb["Yang"]=opt[1]; refreshAlchLbls() end})

Tab7:CreateSection("Herb Year")
Tab7:CreateDropdown({Name="Herb Year to Use",Flag="AlchYear",Options=YEAR_DISPLAY_LIST,CurrentOption="Thousand Year Old",
    Callback=function(opt[1])
        local pick=opt[1]; selectedAlchemyYear=YEAR_TO_VARIATION[pick] or "Thousand"
        if selectedRecipe and ALCHEMY_RECIPES[selectedRecipe] then
            local r=ALCHEMY_RECIPES[selectedRecipe]; local total=calcRecipeBase(r,selectedAlchemyYear)
            if total<r.Base then
                pcall(function() alchLblBaseCheck:Set(string.format(" Base too low! Got ~%d, need %d. Craft will FAIL!",total,r.Base) end))
                Rayfield:Notify({Title=" Herb Year Too Low!",Content=string.format("%s needs base %d, herbs give ~%d. Will fail!",selectedRecipe,r.Base,total),Duration=5, Image=4483362458}))
            else
                pcall(function() alchLblBaseCheck:Set(string.format(" Base OK: ~%d / %d required.",total,r.Base) end))
            end
            refreshAlchLbls()
        end
    end})

Tab7:CreateSection("Amount")
Tab7:CreateDropdown({Name="Amount to Craft",Flag="AlchAmount",Options={"1","2","5","25","100","1000"},CurrentOption="1",
    Callback=function(opt[1]) alchemyCraftAmount=tonumber(opt[1]) or 1 end})

Tab7:CreateSection("Craft")
Tab7:CreateButton({Name=" Craft Once",
    Callback=function()
        if not selectedRecipe then Rayfield:Notify({Title="Alchemy",Content="Please select a recipe first!",Duration=3, Image=4483362458})); return end
        if alchemyActive then Rayfield:Notify({Title="Alchemy",Content="Already crafting, please wait!",Duration=2, Image=4483362458})); return end
        local r=ALCHEMY_RECIPES[selectedRecipe]
        if r then
            local total=calcRecipeBase(r,selectedAlchemyYear)
            if total<r.Base then Rayfield:Notify({Title=" Base Value Too Low!",Content=string.format("Herbs give ~%d base but %s needs %d.",total,selectedRecipe,r.Base),Duration=6, Image=4483362458})); return end
        end
        alchemyActive=true
        task.spawn(function() runAlchemy(alchLblStatus,false) end)
    end})
Tab7:CreateToggle({Name=" Auto Craft (Repeats until stopped)",CurrentValue=false,
    Callback=function(val)
        if val then
            if not selectedRecipe then Rayfield:Notify({Title="Alchemy",Content="Please select a recipe first!",Duration=3, Image=4483362458})); return end
            local r=ALCHEMY_RECIPES[selectedRecipe]
            if r then
                local total=calcRecipeBase(r,selectedAlchemyYear)
                if total<r.Base then
                    Rayfield:Notify({Title=" Base Value Too Low!",Content=string.format("Herbs give ~%d base but %s needs %d.",total,selectedRecipe,r.Base),Duration=6, Image=4483362458})); return
                end
            end
            alchemyActive=true
            task.spawn(function() while alchemyActive do runAlchemy(alchLblStatus,true); if alchemyActive then task.wait(0.5) end end end)
            Rayfield:Notify({Title="Auto Craft ",Content="Auto crafting: "..selectedRecipe,Duration=3, Image=4483362458}))
        else
            alchemyActive=false; pcall(function() alchLblStatus:Set(" Auto Craft Stopped") end)
            Rayfield:Notify({Title="Auto Craft ",Content="Stopped",Duration=2, Image=4483362458}))
        end
    end})
Tab7:CreateButton({Name=" Stop Crafting",
    Callback=function() alchemyActive=false; pcall(function() alchLblStatus:Set(" Stopped") end); Rayfield:Notify({Title="Alchemy",Content="Stopped",Duration=2, Image=4483362458})) end})

Tab7:CreateSection("Recipe Reference")
Tab7:CreateParagraph({Title="Breakthrough Pill",  Content="Base:40 | Earth+Earth+Earth+Earth | Herbs: Ginseng x4"})
Tab7:CreateParagraph({Title="Restoration Pill",   Content="Base:50 | Earth+Earth+Earth+Yang | Herbs: Ginseng x3, Yang Flower x1"})
Tab7:CreateParagraph({Title="Health Pill",        Content="Base:80 | Yin+Earth+Yang+Earth | Herbs: Yin Bush, Ginseng, Yang Flower, Ginseng"})
Tab7:CreateParagraph({Title="Qi Pill",            Content="Base:80 | Heaven+Heaven+Earth+Yin | Herbs: Qilin Berries x2, Ginseng, Yin Bush"})
Tab7:CreateParagraph({Title="Molian Pill",        Content="Base:150 | Yin+Yin+Yang+Heaven | Herbs: Yin Bush x2, Yang Flower, Qilin Berries"})
Tab7:CreateParagraph({Title="Shulian Pill",       Content="Base:150 | Heaven x4 | Herbs: Qilin Berries x4"})
Tab7:CreateParagraph({Title="Dragon Pill",        Content="Base:250 | Earth+Heaven+Yin+Yang | Herbs: Ginseng, Qilin Berries, Yin Bush, Yang Flower"})
Tab7:CreateParagraph({Title="Tian Pill",          Content="Base:275 | Earth+Heaven+Heaven+Earth | Herbs: Ginseng, Qilin Berries x2, Ginseng"})
Tab7:CreateParagraph({Title="Soul Breakthrough",  Content="Base:300 | Yin+Yang+Yin+Yang | Herbs: Yin Bush, Yang Flower, Yin Bush, Yang Flower"})
Tab7:CreateParagraph({Title="Soul Energy Pill",   Content="Base:300 | Yin+Yin+Yang+Yang | Herbs: Yin Bush x2, Yang Flower x2"})
Tab7:CreateParagraph({Title="Soul Chance Pill",   Content="Base:400 | Yang+Yang+Yin+Yin | Herbs: Yang Flower x2, Yin Bush x2"})
Tab7:CreateParagraph({Title="Soul Cap Pill",      Content="Base:300 | Yang+Yin+Yang+Yin | Herbs: Yang Flower, Yin Bush, Yang Flower, Yin Bush"})

-- TAB 8 SETTINGS
local Tab8 = Window:CreateTab(" Settings", "zap")

local antiAfkEnabled = true
local antiAfkConnection = nil

local function startAntiAfk()
    if antiAfkConnection then antiAfkConnection:Disconnect() end
    pcall(function()
        local vu = game:GetService("VirtualUser")
        Player.Idled:Connect(function()
            if antiAfkEnabled then
                vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end
        end)
    end)
    antiAfkConnection = RunService.Heartbeat:Connect(function()
        if antiAfkEnabled and Character and HRP then
            if tick() % 60 < 0.1 then
                pcall(function()
                    local currentPos = HRP.Position
                    HRP.CFrame = CFrame.new(currentPos + Vector3.new(0, 0.1, 0))
                    task.wait(0.05)
                    HRP.CFrame = CFrame.new(currentPos)
                end)
            end
        end
    end)
end

local function stopAntiAfk()
    if antiAfkConnection then antiAfkConnection:Disconnect(); antiAfkConnection=nil end
end

startAntiAfk()

Tab8:CreateSection("Boss Notifications")
Tab8:CreateToggle({Name="Boss Spawn Alert",Flag="BossAlert", CurrentValue=true,
    Callback=function(val)
        bossNotifyEnabled = val
        Rayfield:Notify({Title="Boss Alert", Content=val and "Notifications ON" or "Notifications OFF", Duration=2, Image=4483362458}))
    end})

Tab8:CreateSection("Anti-AFK")
Tab8:CreateToggle({Name="Anti-AFK",Flag="AntiAFK",CurrentValue=true,
    Callback=function(val)
        antiAfkEnabled=val
        if val then
            startAntiAfk()
            Rayfield:Notify({Title="Anti-AFK",Content="Enabled",Duration=2, Image=4483362458}))
        else
            stopAntiAfk()
            Rayfield:Notify({Title="Anti-AFK",Content="Disabled",Duration=2, Image=4483362458}))
        end
    end})

-- PLAYER ESP
local playerESPEnabled   = false
local playerESPObjects   = {}

local function createPlayerESP(plr)
    if plr == Player then return end
    local char = plr.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local hl = Instance.new("Highlight")
    hl.FillColor=Color3.fromRGB(255,50,50); hl.OutlineColor=Color3.fromRGB(255,255,255)
    hl.FillTransparency=0.4; hl.OutlineTransparency=0.1
    hl.DepthMode=Enum.HighlightDepthMode.AlwaysOnTop; hl.Adornee=char; hl.Parent=char
    local bb = Instance.new("BillboardGui")
    bb.Adornee=hrp; bb.Size=UDim2.new(0,120,0,30); bb.StudsOffset=Vector3.new(0,3,0)
    bb.AlwaysOnTop=true; bb.Parent=hrp
    local lbl = Instance.new("TextLabel")
    lbl.Size=UDim2.new(1,0,1,0); lbl.BackgroundTransparency=1
    lbl.TextColor3=Color3.fromRGB(255,80,80); lbl.TextStrokeTransparency=0
    lbl.TextStrokeColor3=Color3.fromRGB(0,0,0); lbl.TextScaled=true
    lbl.Font=Enum.Font.GothamBold; lbl.Text=plr.Name; lbl.Parent=bb
    playerESPObjects[plr] = {highlight=hl, billboard=bb}
end

local function removePlayerESP(plr)
    local obj = playerESPObjects[plr]
    if obj then
        pcall(function() obj.highlight:Destroy() end)
        pcall(function() obj.billboard:Destroy() end)
        playerESPObjects[plr] = nil
    end
end

local function clearAllPlayerESP()
    for plr in pairs(playerESPObjects) do removePlayerESP(plr) end
end

local function refreshPlayerESP()
    clearAllPlayerESP()
    if not playerESPEnabled then return end
    for _, plr in pairs(Players:GetPlayers()) do createPlayerESP(plr) end
end

RunService.RenderStepped:Connect(function()
    if not playerESPEnabled then return end
    local myHRP = HRP; if not myHRP then return end
    for plr, obj in pairs(playerESPObjects) do
        local char = plr.Character
        if not char or not char.Parent then
            removePlayerESP(plr)
        else
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and obj.billboard then
                local dist = math.floor((myHRP.Position - hrp.Position).Magnitude)
                local lbl  = obj.billboard:FindFirstChildOfClass("TextLabel")
                if lbl then lbl.Text = plr.Name .. "\n" .. dist .. "m" end
            end
        end
    end
end)

Players.PlayerAdded:Connect(function(plr)
    if playerESPEnabled then
        plr.CharacterAdded:Connect(function()
            task.wait(1)
            if playerESPEnabled then createPlayerESP(plr) end
        end)
    end
end)

Players.PlayerRemoving:Connect(function(plr) removePlayerESP(plr) end)

Tab8:CreateSection("Player ESP")
Tab8:CreateToggle({Name="Player ESP",Flag="PlayerESP", CurrentValue=false,
    Callback=function(val)
        playerESPEnabled = val
        if val then
            refreshPlayerESP()
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= Player then
                    plr.CharacterAdded:Connect(function()
                        task.wait(1)
                        if playerESPEnabled then createPlayerESP(plr) end
                    end)
                end
            end
            Rayfield:Notify({Title="Player ESP", Content="Enabled", Duration=2, Image=4483362458}))
        else
            clearAllPlayerESP()
            Rayfield:Notify({Title="Player ESP", Content="Disabled", Duration=2, Image=4483362458}))
        end
    end})

Tab8:CreateSection("Fly Settings")
Tab8:CreateSlider({Name="Fly Speed (studs/sec)",Flag="FlySpeed",Range={50,500},Increment=10,CurrentValue=150,
    Callback=function(val) flySpeed=val; saveConfig() end})
Tab8:CreateSection("General")
Tab8:CreateButton({Name=" Stop All Farms",
    Callback=function() stopAll(); Rayfield:Notify({Title="Stopped",Content="All farms stopped.",Duration=2, Image=4483362458})) end})
Tab8:CreateButton({Name=" Destroy GUI",
    Callback=function() stopAll(); stopAntiAfk(); Rayfield:Destroy() end})

-- ============================================================
-- TAB 9 AUTO PILL
-- ============================================================
local TabAuto = Window:CreateTab(" Auto", "zap")
TabAuto:CreateSection("Auto Use Restoration Pill")

local autoPillEnabled       = false
local autoPillThread        = nil
local selectedAutoThreshold = "Low"
local autoPillSlot          = "IT. 1"

local AUTO_THRESHOLDS = {"Low", "Middle", "High", "Expert", "Master", "Peak"}

local function parseStatusProgress(text)
    local cur, max = text:match("(%d+)/(%d+)")
    if cur and max then return tonumber(cur), tonumber(max) end
    return nil, nil
end

local function usePill()
    local args = {
        [1] = "Use",
        [2] = 1,
        [3] = "Restoration Pill",
        [4] = selectedAutoThreshold,
        [5] = autoPillSlot,
    }
    pcall(function()
        game:GetService("ReplicatedStorage").RemoteFunctions.Item:InvokeServer(unpack(args))
    end)
    Rayfield:Notify({Title = "Auto Pill",
        Content = "Used Restoration Pill! (" .. selectedAutoThreshold .. ")",
        Duration=4, Image=4483362458}))
end

-- Direct path from game source code
local function getStatusTextObj()
    return Player.PlayerGui:FindFirstChild("StatsGui")
        and Player.PlayerGui.StatsGui:FindFirstChild("StatusImageLabel")
        and Player.PlayerGui.StatsGui.StatusImageLabel:FindFirstChild("StatusText")
        or nil
end

local autoPillConnection = nil
local function runAutoPill()
    if autoPillConnection then autoPillConnection:Disconnect(); autoPillConnection = nil end
    local obj = getStatusTextObj()
    if not obj then
        Rayfield:Notify({Title="Auto Pill", Content="StatusText not found!", Duration=4, Image=4483362458}))
        autoPillThread = nil
        return
    end
    local pillUsed = false
    -- Use .Changed event — instant, no polling
    autoPillConnection = obj:GetPropertyChangedSignal("Text"):Connect(function()
        if not autoPillEnabled then return end
        local text = obj.Text or ""
        local cur, max = parseStatusProgress(text)
        if cur and max and max > 0 then
            if cur >= max and not pillUsed then
                pillUsed = true
                usePill()
            elseif cur < max then
                pillUsed = false
            end
        end
    end)
    autoPillThread = nil
end

local autoLblStatus  = TabAuto:CreateLabel("Status: OFF")
local autoLblCurrent = TabAuto:CreateLabel("Progress: -")

TabAuto:CreateToggle({Name = "Auto Use Pill",Flag = "AutoPill", CurrentValue = false,
    Callback = function(val)
        autoPillEnabled = val
        if val then
            pcall(function() autoLblStatus:Set("Status: ON") end)
            if autoPillThread then
                pcall(function() task.cancel(autoPillThread) end)
                autoPillThread = nil
            end
            autoPillThread = task.spawn(runAutoPill)
            task.spawn(function()
                local obj = getStatusTextObj()
                while autoPillEnabled do
                    if obj and obj.Parent then
                        pcall(function() autoLblCurrent:Set("Progress: " .. tostring(obj.Text) end))
                    else
                        obj = getStatusTextObj()
                        pcall(function() autoLblCurrent:Set("Progress: " .. (obj and tostring(obj.Text) end) or "not found"))
                    end
                    task.wait(0.1)
                end
                pcall(function() autoLblCurrent:Set("Progress: -") end)
            end)
            Rayfield:Notify({Title="Auto Pill", Content="Started! Grade: "..selectedAutoThreshold, Duration=3, Image=4483362458}))
        else
            autoPillEnabled = false
            if autoPillThread then
                pcall(function() task.cancel(autoPillThread) end)
                autoPillThread = nil
            end
            if autoPillConnection then
                autoPillConnection:Disconnect()
                autoPillConnection = nil
            end
            pcall(function() autoLblStatus:Set("Status: OFF") end)
            pcall(function() autoLblCurrent:Set("Progress: -") end)
            Rayfield:Notify({Title="Auto Pill", Content="Stopped.", Duration=2, Image=4483362458}))
        end
    end})

TabAuto:CreateDropdown({
    Name = "Grade", Flag = "PillGrade", Options = AUTO_THRESHOLDS,
    CurrentOption="Low",
    Callback = function(opt[1])
        selectedAutoThreshold = opt[1]
        saveConfig()
        Rayfield:Notify({Title="Auto Pill", Content="Grade: "..selectedAutoThreshold, Duration=2, Image=4483362458}))
    end})

TabAuto:CreateInput({
    Name = "Pill Slot (e.g. IT. 1)",Flag = "PillSlot", CurrentValue = "IT. 1",
    PlaceholderText = "IT. 1",
    Callback = function(val) if val and val ~= "" then autoPillSlot = val; saveConfig() end end})

TabAuto:CreateButton({Name = " Use Pill Now (Manual)", Callback = function()
    usePill()
end})

-- Load config sekarang
loadConfig()

-- ============================================================
-- LOAD SAVED CONFIG (Rayfield built-in)
-- ============================================================
Rayfield:LoadConfiguration()

-- ============================================================
-- WELCOME
-- ============================================================
Rayfield:Notify({Title="MELAYU-HUB", Content="Loaded by SIGMA.", Duration=6, Image=4483362458})
